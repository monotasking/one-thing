# transport ratchet baseline (主线 T1)
# 只许降不许升。降了之后跑 `bun run transport:gate --write-baseline` 收紧。
# 生成于 2026-08-14；每一行是 `<指标名> <数值>`。
# ---- 2026-08-21 按项重写(结构债 P0.3/P0.5,docs/design/structural-debt-plan-2026-08.md §2)----
# 起点=08-14 基线(channels 337 / bridge 1737 / http 1989 / web 1718 / channels.ts 535)。
# 08-19 `2603c665` 一笔 dump 提交越过了本门;08-21 spaces 整域迁 router 后重测,差额逐项如下:
#  channels:IPC_CHANNELS 337→329:spaces 13 条 invoke 已删(−13);+3 todo-plan 窗口(minimize/zoom/drag,
#    BrowserWindow 操作,属 P4 终态"窗口系残留集",拍板 #10)+ SPACES_CHANGED 广播(router 无推送面)+1;净 −8。
#  lines:web.ts 1718→1707:删 13 条 /api/spaces soft-fail 桩与 softJson;净 −11。
#  lines:bridge.ts 1737→1855(+118):import/type 块 +51、注释 +20、其它包装体 +63(onSpacesChanged 订阅、
#    todo-plan 窗口 3 条、withPlainCommandMentions 展开、OAuth 包装重排),通道引用净 −9 —— 不是新增手写通道。
#  lines:http.ts 1989→2028(+39):L0/L1 server 请求日志(91ab846e),非通道。
#  lines:channels.ts 535→541(+6):+3 todo-plan 窗口常量 +1 SPACES_CHANGED + 注释;−13 spaces。
# 此后基线不再手改;只由 `--write-baseline` 在指标下降时收紧。任何上升 = 红,CI 的 gates job 会拦。
# 2026-08-21(P3'd 路径改名,数值不变):`src/app` 整体成包 `packages/backend`,
#   指标键 `lines:packages/onething-runtime/src/app/server/http.ts` 手工改名为
#   `lines:packages/backend/server/http.ts`;同一个文件、同一个数值,不是收紧也不是放松。
# 2026-08-21(续):practice 域迁 router 后收紧 —— channels 329→319、bridge 1855→1816、web 1707→1681、channels.ts 541→533。
# 2026-08-21(续):collab 域迁 router 后收紧 —— channels 319→304、bridge 1816→1736(已低于 08-14 起点 1737)、web 1681→1666、channels.ts 533→499。
# 2026-08-21(续):P4c 第一批六域(scheduler/variables/app-state/permission/scratchpad/project-dirs)迁 router + 删 server 镜像路由后收紧 —— channels 304→279、bridge 1736→1631、http 2028→1839、web 1666→1577、channels.ts 499→474。
# 2026-08-21(续):P4c 第二批 skills 整域迁 router(+configureShellHost 端口)后收紧 —— channels 279→267、bridge 1631→1575、http 1839→1757、web 1577→1532、channels.ts 474→460。
# 2026-08-22:P4c 第三批 media 11 方法迁 router(5 条字面量通道随之消失,C 3 条留宿主)后收紧 —— channels 267→261、bridge 1575→1532、http 1757→1647、web 1532→1488、channels.ts 460→455。
# 2026-08-22(续):P4c 第四批 session-command 整域迁 router —— 命令总线的入口,`SESSION_COMMAND` 常量与 `POST /api/sessions/:id/commands` 一起消失;收紧 channels 261→260、bridge 1532→1487、http 1647→1637、web 1488→1487、channels.ts 455→454。
# 2026-08-22:P4c 第四批 sessions 26 方法迁 router(22 常量 + 4 字面量通道;为 mobile 留 3 条 REST)后收紧 —— channels 260→238、bridge 1487→1358、http 1637→1496、web 1487→1387、channels.ts 454→437。
# 2026-08-22:P4c 第五批 chat 6 方法迁 router(RESUME_AFTER_TOOL_CONFIRM 按 #21 留手写;/api/streams/abort 为 mobile 留薄适配)后收紧 —— channels 238→232、bridge 1358→1334、http 1496→1461、web 1387→1370、channels.ts 437→431。
# 2026-08-22:P4c 第六批 acp 8 + mcp 16 迁 router(两域零推送,工厂整删;mcp http 侧四道护栏保留)后收紧 —— channels 232→208、bridge 1334→1247、http 1461→1235、web 1370→1311、channels.ts 431→403。
# 2026-08-22:P4c 第七批 themes 5 + oauth 6(2 推送留,走 configureOAuthEventBroadcaster 端口)迁 router 后收紧 —— channels 208→197、bridge 1247→1201、http 1235→1137、web 1311→1287、channels.ts 403→395。
# 2026-08-22:P4c 第八批 gateway 8(configureGatewayHost 端口)+ files 14(http 侧 14 条夹紧逐字保留;FILE_WATCH_EVENT 推送留)迁 router 后收紧 —— channels 197→175、bridge 1201→1126、http 1137→972、web 1287→1230、channels.ts 395→370。
# 2026-08-22:P4c 第九批 tools 7(http 护栏逐字)+ interaction 2 + music 14(两域 web 能力位默认关;4 music 推送留)迁 router 后收紧 —— channels 175→151、bridge 1126→1045、http 972→887、web 1230→1195、channels.ts 370→336。
# 2026-08-22:P4c 第十批 evals 14 + evals-workbench 11 迁 router(app.isPackaged 摘成 configureEvalsHost 端口;3 条进度推送留,改走 configureEvalsEventBroadcaster 且 run 进度由单窗定向改全窗;web 能力位 evals 默认关)后收紧 —— channels 151→126、bridge 1045→936、web 1195→1112、channels.ts 336→307(http 887 不变:evals 从来没有 server 路由)。
# 2026-08-22:P4c 第十一批 settings 4 + voice 11 迁 router(两个「无工厂的漏网 handler」)后收紧 —— channels 126→111、bridge 936→888、http 887→806、web 1112→1081、channels.ts 307→300。
#   settings 留 2 条 C(OPEN_SETTINGS_WINDOW / SHOW_OPEN_DIALOG),推送 SETTINGS_CHANGED 改走 configureSettingsEventBroadcaster;
#   **排除发起窗一字未丢** —— `RpcDispatchContext` 新增一格宿主铸的 `callerId`(@main/ipc/rpc.ts 从 event.sender.id 铸,http 不填),
#   域把它递成 `excludeCallerId`(回灌整份 settings 会冲掉那扇窗正在编辑的草稿,靠幂等兜不住)。
#   voice 两条推送本来就是 configureVoiceHost 端口、一行没动;**VOICE_AUDIO_CHUNK 不迁** —— 高频 PCM 单向上行不带回执,
#   搬到只有请求/响应面的 router 上等于给每块音频加一条空回执(性能面变化),按拍板 #10 归**流式单向残留集**:
#   常量 + preload 的 ipcRenderer.send + 主进程 ipcMain.on 原样留着(@main/ipc/voice.ts 终态小文件)。
# 2026-08-22 #21:删第七条「工具审批后恢复流」的 invoke 整链(渲染层零调用者、引擎 pause 路径已无生产者;两只手写工厂 + runtime ForIpc 助手 + platform 方法一并删,引擎命令总线不动)—— channels 111→110、bridge 888→881、web 1081→1073、channels.ts 300→299(http 806 不变:这条从来没有 server 路由)。
# 2026-08-22 D2:terminal 7 条请求迁 terminal router 域(http 侧结构化拒,能力位仍关);DATA/EXIT 推送留注入广播器;channels 110→103、bridge 881→864、web 1073→1067、channels.ts 299→292,四壳 3029
# 2026-08-23 C2:plugins 19 条 invoke 迁 plugins router 域(http 读面沿用 server catalog、写面按管理器在场;pluginsManage 能力位默认关);NOTIFICATION/REQUEST_PROGRESS 推送留注入广播器;server 6 条 /api/plugins REST 与那条 501 删;channels 103→84
# 2026-08-23 A1-a:立**宿主壳路由** `shell:invoke`(+1 常量)—— 与 `rpc:invoke` 对称的第二条通用通道,
#   处理者住在宿主(`apps/electron/src/ipc/shell/`)而不是装配层。窗口系八域迁入,吃掉 21 条常量:
#   todo-plan-window 7 / search-window 4(3 条窗口动作 + execute-action)/ settings-window 1 / window 1 /
#   dialog 1 / media-window 3 / notify 2 / deeplink 2(ready 与 respond 本就是 invoke,不是推送)。
#   另删两条**死常量**:`CLEAR_CHAT`(全仓零引用)、`UPDATE_SESSION_MAX_TOKENS`(桌面无 handler、渲染层零调用点;
#   server 那条 REST 路由留着)。`search:query` 判定为**数据面**(处理者不碰窗口)留原地,待单独一批迁 backend rpc 域。
#   收紧:channels 84→62、bridge 775→718、web 879→785、channels.ts 267→256,四壳 3040→2489
#   (http 730 不变:窗口系从来没有 server 路由)。
# 2026-08-23 A1-b:browser 19 条 + shell-controller 4 条字面量迁壳路由,SEARCH_QUERY 迁 backend search 域;channels 62→42
#   browser:19 条请求面进 `browserRouter`(宿主壳路由,处理者动的是 WebContentsView 这扇原生视图),
#   只留 BROWSER_TABS_CHANGED 一条推送;旧可移植工厂 apps/electron/src/ipc/browser.ts 整只删。
#   shell-controller:四条**从来不在 IPC_CHANNELS 表上**的字面量通道(shell:open-path / shell:open-external /
#   app:get-data-path / window:set-button-visibility)——前三条进新立的 shellRouter,第四条动的是发起窗本身、
#   进 A1-a 就有的 windowRouter;工厂与它的测试整只删,checker 换成反向棘轮(那四条字面量不许回宿主树)。
#   SEARCH_QUERY:A1-a 判定的那条数据面兑现 —— 进 backend 的 `search` RPC 域,按 context.transport 分叉
#   (ipc 走桌面 executeSearch;http 走 server/search-providers.ts 单槽端口,装的是从前 POST /api/search/query
#   背后的同一个闭包,facade 上 search.query 与那条 REST 一起删,executeAction 留 —— 它是窗口活的 server 对应物)。
#   收紧:channels 62→42、bridge 718→674、http 730→726、web 785→758、channels.ts 256→234,四壳 2489→2392。
# 2026-08-29 React 壳 D0(§5.6 甲案):**两项显式上调,不是新通道**。
#   传输面的通道数一条没动(channels 42 / channels.ts 234 / bridge 674 全部不变),
#   涨的是**基址与 token 的注入能力**——新的 React Electron 壳从 file:// 或自己的
#   vite dev server 加载,没有 apps/web 那层 dev 代理,`/api/...` 同源相对路径对它不成立。
#   lines:web.ts 758→770(+12):`requestJson` 与 `createEventSourceSubscription` 各改走
#     `platform/transport-config.ts` 的单槽端口(缺席=原样相对路径、原样 headers,
#     apps/web 零变化、决不双份注入),其余是解释这条纪律的注释。
#   lines:http.ts 743→775(+32,2026-08-29):CORS 源按请求解析 —— 回环源/null 回显、
#   陌生源仍钉配置值(resolveCorsOrigin + vary),React 壳 5175 被写死 5174 拒掉的修复;
#   零新通道,涨的是解析函数与纪律注释。
#   lines:http.ts 726→743(+17):`checkRequestAuthorization` 多收一个 `url` 形参,
#     **仅** `GET /api/events` 认 `?token=`(EventSource 带不了 header,别无他路),
#     走同一个 `tokenMatches` 常时比较;名单只有这一条,请求日志只记 pathname。
# 2026-08-29 共享层读侧补齐 E 批:**两项显式上调,通道数一条没动**。
#   channels 42 / channels.ts 234 / bridge 674 全部不变 —— 本批没有新通道、没有新 REST 路由,
#   涨的是既有那条 `GET /api/events` 上多下发**一种具名 SSE 事件**,以及 web 壳把对应的
#   空桩换成真订阅。
#   lines:http.ts 775→786(+11):`handleEvents` 里多一段 `settings:changed` 的订阅转发
#     (`runtime.settings?.subscribeChanged` 在场才挂)。它**不进合批器、不占会话事件的 seq**
#     —— 重连的 Last-Event-ID 只对会话事件序号有意义,注释就写在那一段上面。
#     不新开 `/api/settings/events`:那会同时长一条路由与一条壳税,而这条推送与会话事件
#     天然同一批订阅者(打开页面就要听)。
#   lines:web.ts 770→775(+5):`onSettingsChanged` 从 `() => () => {}` 换成
#     `createEventSourceSubscription("/api/events", "settings:changed", cb)`(+ 一个类型 import
#     与三行解释)。桌面侧 `SETTINGS_CHANGED` 一行未动。
#   会话生命周期(建 / 删)那一半**零行变化**:建骑既有的 `session:ledger-event`,
#   删新增一条**会话事件词**(`SESSION_EVENT_TYPES.SESSION_REMOVED`,不是 `IPC_CHANNELS` 常量),
#   渲染侧的折叠住在壳外一个模块 `platform/session-lifecycle.ts` —— 四壳一行不动。
channels:IPC_CHANNELS 42
lines:packages/backend/server/http.ts 742
lines:packages/shared/ipc/channels.ts 234
