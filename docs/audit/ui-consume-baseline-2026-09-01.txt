# ui:consume 基线 —— 09-01「基础件先行」立法批交卷时的存量
#
# 打全表:apps/desktop-react $ node scripts/ui-consume-check.mjs
# 判棘轮:apps/desktop-react $ npm run ui:consume
#
# 比对键是**规则 + 文件 + 第几条**,不含行号(上下加一行不该算成新增)。
# 只减不增:violation 与 debt 两档同样,新增即红。修掉一条就把那一行删掉。
#
# ⚠️ 这份基线是**多批并行期的快照**:立门时 src/content/**(查看器/文件面批)
#    正在同一棵树上迁它自己的裸钮,截取之后又少了十几条。门对「少了」永远是绿的
#    (只会提示「这几条可以从基线删掉」),所以快照偏旧只会让基线虚胖,不会误红。
#    **合树收账时**跑一次 `npm run ui:consume`,把它列出来的 “已经不在了” 逐行删掉。
#
# ── 存量为什么留着(逐类理由,09-01)────────────────────────────────
# bare-button-icon / -text  55 条:该消费 ui/IconButton · ui/Button 的存量。
#   IconButton 本身是**别批在飞**的新件(未入库),迁移会与六条在飞的批冲旁改;
#   本批只立门不搬存量(派工令原话)。
# bare-button-structural    24 条:瓦/卡/行/琴键这类视觉本该定制的件,
#   不违例,但欠一笔 ui/ButtonBase(本批新立的「只清 UA」极小件)。
#   本批已把自己碰过的 5 处迁完(composer 两抽屉 + 模型抽屉 + 快切两行)。
# tooltip-native-title      12 条:禁 native title= 的存量。分散在 Dock/查看器/
#   总览/研究段/表格,全部落在别批在飞的避让区。
# icon-button-hover          1 条:composer 自绘的 .toolBtn:hover。它归本批的
#   地界,但修法是迁 ui/IconButton —— 那件在飞,所以留账不留改。
# kbd-select-handwritten     3 条:手写 ↑↓ 走行,该迁 ui/a11y/list-selection。
#   三处全在避让区:检索面板(检索批)、查看器跳转条(查看器批)、
#   总览网格(它是二维网格导航,与候选列表不同形,迁法要单独设计)。
#   **09-01 批 4 清零**(见文件末「批 4」一节):检索面板迁 useListSelection、
#   总览那条是规则误报写了豁免、跳转条由查看器批自己修掉了。
# kbd-select-hover           0 条 —— 本批清零,这正是立门的起因。
#
# ── 09-01 批 3(裸钮与 native title 清账 · 避让区外全量),删 51 条 ────────────
#   四类在**避让区外**清零:bare-button-text 8→0、bare-button-icon 24→**1**、
#   bare-button-structural 13→0、tooltip-native-title 6→0、icon-button-hover 1→0。
#   (避让区 = 别批在飞的 src/content/** 与 src/ui/**;src/providers/** 已收编完。)
#
#   三类判逐处落地:文字动作钮 → ui/Button;图标钮 → ui/IconButton;
#   瓦 / 卡 / 行 / 琴键 / 选项 / 通栏条这类**结构性交互件** → ui/ButtonBase
#   (只清 UA,本地皮肤一个像素不动)。四块面的自绘图标钮皮肤整份退役
#   (StageOverlay / CoverLayer / FloatWindow / QuickLook 的 `.close` · `.action`,
#   composer 的 `.toolBtn:hover` —— 后者是 icon-button-hover 唯一那条命中)。
#   native title= 的六处换 ui/Tooltip(QuickLook 三 / SessionCard 二);
#   DockTile 那个 `title=""` 直接删 —— 空串从来不显示任何提示,而瓦自己已经有
#   一枚 300ms 的名字标签。
#
#   ── 本批**当场停**的一条(库件缺口,故意留在表上)──────────────────────
#   bare-button-icon src/composer/components/Composer.tsx —— 发送 / 停止键。
#     它该迁 ui/IconButton,但那件只收 `testId` 一格自定义属性,
#     `data-mode={busy ? 'stop' : 'send'}` 递不进去,而 scripts/gate-chat.mjs
#     与 Composer.test.tsx 都逐字读它。缺口 = **IconButton 不透传
#     ButtonHTMLAttributes**(它也不把事件交给 onClick)。补上之后一并迁。
#
#   照旧留着的:两条 kbd-select-handwritten(总览网格 / 检索面板)属批 4;
#   16 条 content/** 与 providers/** 的陈账不归本批动 —— 它们是别批已经修掉、
#   等各自合树时自己删的行。
#
# ── 09-01 批 3.5(库件补口 + 发送键收编),删 1 条 ─────────────────────────
#   上面那条**当场停**结清:`src/composer/components/Composer.tsx` 的发送 / 停止键。
#   缺口 A 补上了(ui/IconButton 现在摊 ButtonHTMLAttributes,onClick 收事件),
#   但这一处**裁定不迁 IconButton**:发送键是 accent **实底圆**的签名件,而
#   IconButton 是「檐上那种钮」(无边框、无实底、只换字色)——给它开一档实底主色
#   等于给它长一整个实底家族,它立件的前提(配方单一)当场没了。
#   落地按裸钮三类判第③类:**结构性交互件 → ui/ButtonBase**(只清 UA),
#   `.sendBtn` 那份 accent 圆底皮肤原样留在 Composer.module.css 当签名件皮肤,
#   `data-mode` / `aria-label` / 手势 props 经 ButtonBase 原样透传 ——
#   scripts/gate-chat.mjs 与 Composer.test.tsx 逐字读的那几格一个字没动。
#
#   至此**避让区外四类全清零**:bare-button-text 0 / bare-button-icon 0 /
#   bare-button-structural 0 / tooltip-native-title 0 / icon-button-hover 0。
#   表上剩下的 bare-button-* 与 tooltip-native-title 全部落在 src/content/**
#   与 src/providers/**(别批在飞,等各自合树时自己删)。
#
# ── 09-01 批 4(键盘走行迁原语 + 层叠浮层 Esc 单层退),删 4 条 ────────────────
#   ① kbd-select-handwritten 清零(表上原本 2 条,跳转条那条已由查看器批修掉):
#      · src/search/components/SearchPanel.tsx —— **真迁**。手写的 `moveRow` 换成
#        `ui/a11y/list-selection` 的 `useListSelection`(受控档,`cursor` 仍是本地
#        state:渲染中的「换词就归零」与底部「加载更多」那条伪行都要读写它)。
#        三个档位逐条对着现状填,不取原语默认口味:loop:false(到端点就停)、
#        homeEnd:false(Home/End 留给输入框的行首行尾)、scrollBlock:null
#        (滚入视野仍由本地那一条 effect 管 —— 底部那条 item 不是一个下标,
#        进不了原语按下标认行的那张表,拆成两半反而多一处接缝)。
#        `search/transitions.ts` 的 `moveRow` 因此**零消费者**,只剩自己的单测
#        吊着;它不在本批地界,留账给 search 批清尸。
#      · src/expose/components/Overview.tsx —— **规则误报,写豁免**。规则按
#        「文件里有 'ArrowDown' 字面量却没 import a11y 原语」认人,而这块面里那两个
#        字面量的全部工作是 `focusGrid() + blur()`:把键盘交给网格,一格下标都不动
#        (既有用例逐字钉着「交接那一下只点亮锚点,不顺手再走一步」)。真正的走行在
#        ExposeView 上,那是**二维网格**(四向 + 按列宽换行),与 useListSelection
#        管的一维候选列表不同形,迁法要单独设计,且仍在别批避让区。
#      两面各补一条「mouseenter 后 active 不变」的守卫(palette.test 先例),
#      各自跑过「加一句 mouseenter 写 active 就红」的反证。
#   ② float-handwritten 4 → 2:
#      · src/composer/components/Composer.tsx —— **真迁**。它就是原语说的点外关。
#        两个新档位都是为保住现状:`outside:'capture'`(照旧捕获阶段 —— 屏幕别处的
#        Select/Tabs/FloatWindow/AgentChip 各有一句 onPointerDown stopPropagation,
#        冒泡档在那些地方点下去收不到)、`escape:false`(这块面的 Esc 是自己那三层,
#        不认 Esc 也就不进浮层栈,不会挡住别人)。
#      · src/content/KeymapSettings.tsx —— **不是浮层散场,写豁免**。那条捕获相位
#        keydown 是**录制**:把录制态里的每一下按键都截住,好让 ⌘P 这种已绑组合
#        录得进来;Esc 在那里是一条录制结果('cancel')而不是关闭。这一格没有浮层。
#      剩下的 JumpBar 与 ExposeView 仍在 src/content/** / 舞台盖的避让区,原样留账。
#   同批在 ui 库里立的新机制(不进本表,记在这里备查):`ui/float` 的**浮层栈** ——
#   层叠时只有最上面那一层认领 Esc(判据:DOM 包含优先,平手看入栈序),
#   `ui/Dialog` 的手写 Esc 段随之退役,改消费 useFloatDismiss 的 `outside:false` 档。
#
# ── 09-02 批 6(content 区收尾),96 → 40 条,删 56 条 ──────────────────────
#   src/content/** 此前是别批的避让区,本批解封收尾。删掉的 56 条分两笔:
#     · **陈账 16 条** —— 上面那句「合树收账时把『已经不在了』逐行删掉」的兑现。
#       FilesPanel / FileViewer / FileDetailPopover / JumpBar / ModelCatalog /
#       ProviderRail 各自那几条早被别批修掉了,基线一直虚胖着。
#     · **本批真消 40 条** —— 逐类清零:
#       bare-button-icon 6→0、bare-button-text 7→0、bare-button-structural 6→0、
#       tooltip-native-title 6→0、float-handwritten 2→1、async-busy-boolean 2→0、
#       shared-vocab-css content 那 12 条 → 0。
#
#   ① 裸钮三类判(19 处)。落在 content 的这一批**绝大多数是第三类**:
#      檐上 / 脚注 / 读数带上的微型静默件(fs-micro、零底零框、图标与字一体),
#      视觉本该定制 —— 与批 3 把「加载更多」判进基座是同一形,换成 `ui/Button`
#      那种 28 高描边钮会把消息流里的注脚做成实体按钮,那不是等价替换。
#      真图标钮只有两颗,都迁 `ui/IconButton`:表格列头的 ⧉ 复制列、块檐上的 ⋯。
#      **这一判请编排复核**:三类判的边界(「重试」既是 ButtonBase.tsx 文件头
#      举的①类例子,又确实长成一句 fs-micro 的脚注)是本批唯一的自由裁量处。
#
#   ② tooltip-native-title 6 处全换 `ui/Tooltip`(cloneElement 注入,零 DOM 变化):
#      行内链接的 href、表格列头、研究段的流中态行与来源行两形、工具行的全名。
#
#   ③ float-handwritten:`content/viewer/JumpBar.tsx` **真迁**(判据同批 4:
#      它是一层开在查看器上的临时面,「这一下 Esc 属于最上面哪一层」正是浮层栈
#      答的问题)。`outside:false` 保住现状(跳转条不点外关)。
#      剩下的 `expose/components/ExposeView.tsx` 照旧留账 —— 批 4 已判它与退层链
#      纠缠、迁法要单独设计。
#
#   ④ async-busy-boolean 两条:
#      · `data/viewer-source.ts` —— **真迁**。`edit.saving` 那颗共享布尔进了
#        `viewerSaveMutation`(createMutation,key = `save:<path>`),三格语义
#        (conflict / error / savedAt)逐字保留;确认对话框那颗「保存并关闭」
#        本来就是 `ui/Button`,随之换成 `ui/AsyncButton`(零像素 + 150ms 防闪)。
#        状态栏那颗**如实偏离不换 AsyncButton**:那件的身子是 28 高的 Button,
#        而那是 26 高读数带上的 mono 动作链接,换过去不是等价替换 —— 理由写在
#        FileViewer.tsx 该处;律③要的禁用 + 换字一件不少,忙态产地已经是逐格。
#      · `data/chat-source.ts` —— **规则误报,写豁免**(理由压在命中上方 8 行内):
#        那是模块级 LiveFold 的折叠机内部闸门,没有任何控件读它。
#
#   ⑤ shared-vocab-css content 12 条,逐条判:
#      · NotificationsPanel 的 `.dot` 五档 → **真迁 `ui/StatusDot`**(几何同为
#        6px 圆、五档底色一格不差)。为此给库件补了第六档 tone `info`:那面画的
#        是四档命运(ok/info/warn/error),而库件当时只认得三档 —— 缺的那一档
#        逼消费方要么把「一条提示」染成 idle(最淡的一档墨),要么继续自绘。
#        `--info` 本来就是 palette 的第四档语义色,补的是已经存在的事实。
#        `--notify-dot` 随之清尸(与批 2c 删 `--pv-dot` 同一笔账)。
#      · NotificationsPanel 的 `.groupHead` → **改名 `.bucketLabel`**:它是一句
#        小节标(小字/撑开字距/英文小写/text-3),与 `ui/GroupHead` 那件
#        「组名 text-1 + 右侧读数 + caret + 可折叠」不同形。
#      · `content/mocks.module.css` 的 `.field` 一族(4 条)→ **改名 `.settingRow`
#        一族**:它是**横排**设置行,而 `ui/Field` 的形是竖排(标签在上、控件在下、
#        底下挂 hint 与 error)。判据早就写在 Field.module.css 的文件头里。
#      · `content/FileGlyph.module.css` 的 `.badge` → **改名 `.ext`**:它是扩展名
#        字标(小圆角矩形 / mono 700 / 品牌色当数据),`ui/Badge` 是计数丸。
#      三处改名都是零像素:改的是名字,一条规则的值都没动。
#
#   ⑥ 同批兑现的两条禁令(不在这张表上,记在这里备查):
#      · **Spinner 只许出现在按钮内或状态栏** —— 内容区四处退成文字:
#        FileViewer 首载、FileDetailPopover 详情加载、ToolRow 行内、
#        ResearchSegment 流中态行。后两处逐处判过「算不算状态栏」:都不算
#        (它们长在聊天正文流里),而且两处本来就已经有文字在说同一件事,
#        ToolRow 那一处还叠着图标呼吸,转圈是第三遍。
#        **状态栏那一颗留着**(FileViewer 脚下 26 高读数带,允许的两处之一)。
#      · i18n 立 `common.saving`(zh 正在保存… / en Saving…),`viewer.saving`
#        随之删:那句话本来就是任何一颗异步钮的忙态话,被三处跨域借用过。
#
#   ⑦ 本批读过但**没有迁**的一处(留账待独立一批):
#      `content/FilesPanel.tsx` 的 `NoWorkdirNotice` 那颗 `useState(busy)`。
#      它护的是 `sessions-source` 的 `setWorkingDirectory` 后端写,按律③该走
#      mutation;没迁是因为产地不在 content —— 那一口的 settle 要做的重拉两句
#      关在 zustand creator 闭包里,模块级够不着,迁它得先把 sessions-source 的
#      重拉那一半提到模块级。理由逐条写在该处注释里。
#
shared-vocab-css src/components/AgentChip.module.css:16 视觉词汇多产地(chip),待批 2 立件收编
shared-vocab-css src/components/AgentChip.module.css:30 视觉词汇多产地(chip),待批 2 立件收编
shared-vocab-css src/components/AgentChip.module.css:34 视觉词汇多产地(chip),待批 2 立件收编
shared-vocab-css src/components/DockTile.module.css:71 视觉词汇多产地(badge),待批 2 立件收编
shared-vocab-css src/components/DockTile.module.css:91 视觉词汇多产地(dot),待批 2 立件收编
shared-vocab-css src/components/DockTile.module.css:99 视觉词汇多产地(dot),待批 2 立件收编
shared-vocab-css src/components/DockTile.module.css:100 视觉词汇多产地(dot),待批 2 立件收编
shared-vocab-css src/components/DockTile.module.css:101 视觉词汇多产地(dot),待批 2 立件收编
shared-vocab-css src/components/DockTile.module.css:102 视觉词汇多产地(dot),待批 2 立件收编
shared-vocab-css src/components/HostTitle.module.css:8 视觉词汇多产地(dot),待批 2 立件收编
shared-vocab-css src/composer/components/Composer.module.css:394 视觉词汇多产地(chip),待批 2 立件收编
shared-vocab-css src/composer/components/Composer.module.css:426 视觉词汇多产地(pill),待批 2 立件收编
shared-vocab-css src/composer/components/Composer.module.css:482 视觉词汇多产地(meter),待批 2 立件收编
shared-vocab-css src/composer/components/Composer.module.css:499 视觉词汇多产地(meter),待批 2 立件收编
shared-vocab-css src/composer/components/Composer.module.css:503 视觉词汇多产地(meter),待批 2 立件收编
shared-vocab-css src/composer/components/Composer.module.css:509 视觉词汇多产地(meter),待批 2 立件收编
shared-vocab-css src/composer/components/Composer.module.css:513 视觉词汇多产地(meter),待批 2 立件收编
shared-vocab-css src/composer/components/Composer.module.css:518 视觉词汇多产地(meter),待批 2 立件收编
shared-vocab-css src/composer/components/Composer.module.css:524 视觉词汇多产地(meter),待批 2 立件收编
float-handwritten src/expose/components/ExposeView.tsx:141 手写 Esc 捕获相位认领,该消费 ui/float useFloatDismiss
shared-vocab-css src/expose/components/Overview.module.css:152 视觉词汇多产地(groupHead),待批 2 立件收编
shared-vocab-css src/expose/components/Overview.module.css:264 视觉词汇多产地(groupHead),待批 2 立件收编
shared-vocab-css src/expose/components/QuickLook.module.css:67 视觉词汇多产地(chip),待批 2 立件收编
shared-vocab-css src/expose/components/SessionCard.module.css:10 视觉词汇多产地(card),待批 2 立件收编
shared-vocab-css src/expose/components/SessionCard.module.css:39 视觉词汇多产地(card),待批 2 立件收编
shared-vocab-css src/providers/components/UsageCard.module.css:63 视觉词汇多产地(meter),待批 2 立件收编
shared-vocab-css src/providers/components/UsageCard.module.css:69 视觉词汇多产地(meter),待批 2 立件收编
shared-vocab-css src/providers/components/UsageCard.module.css:79 视觉词汇多产地(meter),待批 2 立件收编
shared-vocab-css src/providers/components/UsageCard.module.css:88 视觉词汇多产地(meter),待批 2 立件收编
shared-vocab-css src/providers/components/UsageCard.module.css:95 视觉词汇多产地(meter),待批 2 立件收编
shared-vocab-css src/providers/components/UsageCard.module.css:105 视觉词汇多产地(meter),待批 2 立件收编
shared-vocab-css src/providers/components/UsageCard.module.css:112 视觉词汇多产地(meter),待批 2 立件收编
shared-vocab-css src/search/components/SearchPanel.module.css:139 视觉词汇多产地(chip),待批 2 立件收编
shared-vocab-css src/search/components/SearchPanel.module.css:158 视觉词汇多产地(chip),待批 2 立件收编
shared-vocab-css src/workspace/components/WorkspaceOverview.module.css:54 视觉词汇多产地(card),待批 2 立件收编
shared-vocab-css src/workspace/components/WorkspaceOverview.module.css:67 视觉词汇多产地(card),待批 2 立件收编
shared-vocab-css src/workspace/components/WorkspaceOverview.module.css:73 视觉词汇多产地(card),待批 2 立件收编
shared-vocab-css src/workspace/components/WorkspaceOverview.module.css:78 视觉词汇多产地(card),待批 2 立件收编
shared-vocab-css src/workspace/components/WorkspaceOverview.module.css:136 视觉词汇多产地(field),待批 2 立件收编
shared-vocab-css src/workspace/components/WorkspaceOverview.module.css:143 视觉词汇多产地(field),待批 2 立件收编
