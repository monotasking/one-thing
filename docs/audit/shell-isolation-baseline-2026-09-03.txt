# 壳不许 import 别的壳 —— 只减不增的棘轮基线(C0 立表 / C1 清零,
# docs/design/client-sdk-2026-09.md §3 / §7)
#
# 一行一个文件:`<路径> <这个文件里跨壳 import 的条数>`。判据由
# `scripts/headless-boundary-check.ts` 的 `checkShellsDoNotImportOtherShells()` 采:
# 只看 **import 说明符**(注释与字符串先剥掉),命中
# `@renderer/…` 或 `packages/renderer/…`,含动态 `import()` 与 `import type`。
#
# 规矩:**任一文件高于这里的数、或出现这里没有的文件 → 红**。
#
# ── C1 已清零(2026-09-03),这张表下面一行都没有 ────────────────────────────
# 采于 C0 那天是 19 个文件 / 39 条(那就是方案 §5.1 的施工清单)。C1 把它们逐条改成
# `client.api(router)` / `client.events.on(...)` / 从 `@onething/client` 引判据与类型,
# 并删掉了 apps/desktop-react 的 `@renderer` 与 `@/` 两条别名(tsconfig + vite.config)。
# 表空了,**于是这条规则现在就是零基线硬闸** —— 与 boundary:gate 的其它零基线规则
# 同形:任何一条跨壳 import 当场红,checker 一行未改(`baseline.get(file) ?? 0`)。
#
# 反方向(`packages/renderer` → `apps/desktop-react`)一直是 0,checker 里直接硬闸,
# 从来不在这张表上。
#
# 这张表**只减不增**。往里加一行 = 有人又把两个壳接了起来 —— 那是一次拍板,
# 不是顺手件。
