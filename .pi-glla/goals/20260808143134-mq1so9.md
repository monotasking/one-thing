# Goal

**Status**: paused
**Policy**: goal
**Auto-continue**: on
**File**: `20260808143134-mq1so9.md`
**Pause reason**: auditor retry: auditor worker exited without an atomic result
**Agent suggests**: Auto-retry in 120m — or /goal resume to retry now
**Completion audit**: quota-waiting
**Completion audit attempt**: `audit-msknpqn8-3hmp2y`

## Objective

> 实现 MCP 工具混合平铺模式（路线图 docs/design/mcp-upgrade-2026-07-28.md 决策点 #1）：当已连接 MCP 工具总数 ≤ 阈值（默认 20，写入 settings schema 可调，0 = 永远走 router 即现状）时，每个 MCP 工具以独立模型侧定义直接进入 tools 列表（id `mcp:serverId:toolName`，参数来自工具 inputSchema，执行直连 callTool），且不暴露 `mcp_search`；超过阈值时回退为单一 router 工具（现状）。两种模式互斥。模式随 connect/disconnect/list_changed 触发的重规划动态切换（复用 P2-1 扇出路径）。权限模型不变（沿用 per-server 授权）。明确不做：settings UI 控件（设置项落在 settings 文件/schema 即可）、MCP Apps、Tasks。

## Verification contract

bun run typecheck:node 无 error
bun run typecheck:web 无 error
bun run test 全绿（含新增单测，覆盖阈值边界：工具数 ≤20 平铺且无 mcp_search、21 回退 router-only、阈值 0 永远 router、router 被 toolsSettings 禁用时的 skip 行为）
settings schema + defaults 中存在平铺阈值设置项且默认值为 20（grep 可证）
真机冒烟脚本通过：fake Streamable HTTP server + 真实 MCPClient 连接 → 桥接规划在 2 工具时产出 2 个独立模型侧定义且无 mcp_search，平铺工具执行直达 server；工具数加到 21 重规划后翻回 router-only
bun scripts/headless-boundary-check.ts 失败数保持基线 13
docs/design/mcp-upgrade-2026-07-28.md 决策点 #1 标注为已落地（含实现说明）
