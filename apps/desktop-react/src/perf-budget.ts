/**
 * 性能预算 —— **一份常量表,两个消费者**:dev HUD 的报警色,和
 * `scripts/gate-perf.mjs` 的红绿判据。
 *
 * 放在 src 根而不是 services/ 下,是因为它不是一项服务,是一张**契约表**:
 * 「这块壳认为多慢算慢」。HUD 和门都读它,所以门红了不会有人辩「HUD 上是绿的」。
 * 门是 node 脚本、读不了 CSS module,所以这份表必须是纯数据、零 import。
 *
 * 数字的来历(不是拍脑袋):
 *  · 70ms  —— 高频往复交互(切 tab 这类一天几十次的动作)的 p95 上限。100ms 的
 *    RAIL 线只是「按了没反应」的下限,不是「感觉利索」的线:08-30 用户报切换钝,
 *    量出来 83ms —— 在 100 的预算带里绿着,手感却是每次错过约 5 帧。所以这一档
 *    改按**健康读数锚定**:修完打包版 p95 61ms,预算 = 61 + 一成五抖动余量 ≈ 70
 *    (60fps 下约 4 帧)。再回到 80ms 档,门先于用户开口变红。
 *  · 100ms —— RAIL 的交互响应上限,留给**一次性**的重交互(冷开几百张卡的首屏):
 *    它是「按了等它开」,不是往复动作,按「没反应」的线卡就够了。
 *  · 393ms(sessionSwitchMs,09-03 二修按读数锚定,不再是占位)—— 切会话是
 *    「整棵消息树换一份」:上一条的树要拆、新的一条要解析 markdown 再排版,
 *    与①「几百张卡首屏」同属一次性重交互,但比它重一档。所以它不套
 *    `interactionP95Ms`(那是切 tab 那种一天几十次的往复动作),自己一格。
 *    读数:批 A 修完(clamp-measurer 共享 RO / useChatToc 合帧 / assemble 按身份章
 *    LRU)在打包版上连跑 4 次 `gate-perf.mjs`,p95 291/341/299/273ms —— 抖动带本身
 *    有约 70ms 宽,是这台机器上后台进程与 GC 的噪声,不是产品代码的确定性行为
 *    (下面 `sessionSwitchForcedLayouts` 才是)。锚点取 4 次里的**最大值** 341,
 *    乘一成五抖动余量 ≈ 392.15,取整 393 —— 与①同一条算法,只是这一档更宽的
 *    抖动带要求锚在最大值而不是随手一次的读数上,否则下一次跑门会被自己的
 *    噪声打红。
 *  · 3 次(sessionSwitchForcedLayouts)—— **这一档真正的红绿判据**(见
 *    `scripts/gate-perf.mjs` 里 `forcedLayouts()` 的大注:时间 p95 落在改前改后
 *    重叠的抖动带里,拿它当判据会闪;强制排版次数是一个**整数计数**,同一次
 *    改动前后在这台机器上跑多少遍都不变)。09-03 批 A 修完是 3;W1-b 之后是 4;
 *    **W7-c 收回 3** —— 多出来那一次的名字(`useLeafGeometry` 的每渲染重量)随
 *    裁定 1 一起退役了。这一格的意义是**棘轮**,不是「多少算快」。
 *  · 50ms  —— Long Animation Frame 的定义线,也是 INP「长任务」的口径;
 *  · 33ms  —— 30fps 的一帧。流式回放是**稳态**动画,掉到 30fps 以下就看得出顿。
 *    (不用 16.7ms/60fps 是刻意的:流式期间主线程本来就有真实工作要做,
 *     按 60fps 卡会把正常的解析开销也判成红。)
 */

export const PERF_BUDGET = {
  /** 高频交互延迟(event timing 的 duration)的 p95 上限,毫秒。 */
  interactionP95Ms: 70,
  /** 一次性重交互(冷开会话总览这类首屏全画)的端到端上限,毫秒。 */
  coldOpenMs: 100,
  /** 切会话(常规档 ↔ 常规档)「按下 → 上屏」的 p95 上限,毫秒——参考读数,见下方大注。 */
  sessionSwitchMs: 393,
  /**
   * 切会话单次允许出现的强制排版(forced reflow)次数上限——**这一档真正的判据**,
   * 见下方大注。
   *
   * **09-06(W7-c)从 4 收回 3。** W6-p 把它从 3 抬到 4 时,多出来那一次是有名有姓的
   * 一件**正当**的量:`useLeafGeometry` 的每渲染重量(`workbench/leaf-geometry.ts`,
   * W1-b 加的)—— 顶栏那组标签要精确坐在它那片叶的正上方,而「位置变了尺寸没变」
   * 那一形 `ResizeObserver` 一声不吭,只有渲染即重量接得住(读数 2.1ms / 11 个元素)。
   *
   * W7-c 裁定 1 把那条规则整条退役了(v3 单叶之后中央区只有一组标签,「对准哪片叶」
   * 没有对象;标签改从顶栏自己的开头排),`leaf-geometry.ts` 随之删除 —— 于是那一次
   * 量**不再发生**,不是被优化掉的,是那件事没有了。真机复量:8 次切换逐次
   * `3 3 3 3 3 3 3 3`,一次不抖。留下的三次逐条归因(trace 的
   * `args.beginData.stackTrace` 顶帧):
   *  1. 工具卡的高度翻页效果那只 layout effect(读 `offsetHeight`),0.2–0.5ms;
   *  2. `ChatStream` 的**贴底**(`stick()` 读 `scrollHeight`)—— 换会话那一次提交,
   *     76–100ms(它就是这一格里最贵的那一次,而它是这一下的**正事**);
   *  3. 同一只 `stick()` 的第二次 —— 内容分两批到齐(切会话之后传输那一头还会再送一批),
   *     于是又提交一次、又贴一次底,27–29ms。
   * 三次里没有一次是「读了不该读的东西」:1 是动效、2 与 3 是同一件正事的两批到货。
   * **所以这一格的意义仍旧是棘轮** —— 3 是今天这份代码的确定性读数,不留余量;
   * 第 4 次一出现就红,而那时要问的第一句话仍旧是「它是谁」。
   */
  sessionSwitchForcedLayouts: 3,
  /**
   * **二合一 / 拆开**「点下 → 稳定」的 p95 上限,毫秒(W6-p;这一格从前叫
   * `focusLeafSwitchMs`,量的是 W5-b 的「切焦点叶」——中央区收成一条标签条之后
   * 那个现场在结构上不存在了,夹具每趟都红在「中央区真成两片叶」上)。
   *
   * 它量的是**改标签的身份**这一下:两格普通标签并成一格 pair、再拆回来。按设计
   * §11 拍点 8,这一下**一格内容都不许重挂**(内容层按内容分格,改的只有画法那一半),
   * 所以它理应几乎不花钱。与 `sessionSwitchMs`(那片叶的内容整份换人)是两件事。
   *
   * 时间只当参考(它随机器抖),红绿的主判据是下面那格强制排版次数 —— 与
   * `sessionSwitchMs` / `tabReorderMs` 逐字同一条立法理由。
   *
   * **首次读数**(2026-09-05,本机 `gate:perf` 场景⑤b,10 下奇并偶拆):
   * 逐下 `305 250 233 253 253 247 280 239 253 233`,p95 305ms。预算 = 最大值 305 ×
   * 一成五抖动余量 ≈ 351 —— 与 `sessionSwitchMs` 同一条算法。
   *
   * ── **09-06(W7-d 裁定 3):这一格判的不再是 p95,是这一趟的地板** ──────────
   * 病历:W7-c 交卷时三趟读数 `301 / 592 / 335`(预算 351)—— 中间那一趟不是回归,
   * **任务数在同一格窗口里从 2766 抖到 101359**:这台机器连跑几小时门之后的负载。
   * 一条门贴着自己的噪声线,红绿就变成掷硬币,而掷出来的红会耗掉一整轮排查。
   *
   * 裁定**不许抬预算**(351 一个字没动),改的是**读哪个数**:
   * 门从这一趟 10 下里取**最快的那一下**(地板)与预算比,不再取 p95。
   *  · 为什么地板接得住真回归:回归是「这一下变贵了」,它把**每一下**都抬起来 ——
   *    最快的那一下同样抬。W6-b 那次真回归的现场是 259/501/210/448,地板 210 也远在
   *    首次读数的 233 之上一档就会显形;
   *  · 为什么地板挡得住噪声:负载抬的是**尾巴**(GC、后台进程抢核),10 下里总有
   *    几下落在干净的窗口里。这与 `tabReorderMs` 那一格「一趟的地板不归产品管」是
   *    同一个观察的两面:确定性的那一半在下界,抖的那一半在上界。
   * 逐下读数照旧全打出来(p95、最快、最慢),**判的只有最快那一下**。
   *
   * 同一条理由把「最长主线程任务 ≤ 这一格」那条断言也降成了参考打印:它是**整趟
   * 窗口**里的单个极值(还包含每下之间那 400ms 的等待),被负载污染得比 p95 更彻底,
   * 而它想接住的那件事(「一下变成两下」)地板与强制排版计数已经各接一遍。
   *
   * **这一格为什么天生是百毫秒级,而不是「几乎不花钱」**(归因,同一趟的 trace):
   * 一下的钱在**一次真的重排**上,不在重挂上 —— 拍点 8 兑现了(内容节点是同一个,
   * `content-slots` 只把 holder `appendChild` 进另一个槽),但**那一格地变窄了一半**:
   * 33082 个元素重算样式(32.7ms)+ `ChatStream` 的贴底(`stick()` 读 `scrollHeight`)
   * 当场逼出 **149.7ms** 的排版。一条 437KB 的会话按半幅重新折行,这笔钱是这一下的
   * **正事**,不是浪费。所以这一格**没有「零长帧」那条断言** —— 它做不到,写上去就是
   * 一条永远红的假线;门改成断言「最长主线程任务 ≤ 这一格的时间预算」(与 ⑤a 第三条
   * 同形),能接住「一下变成两下」这种真回归。要把它压下去得让看不见的消息不参与排版
   * (聊天区的 containment),那是 R 线的事,本批只把账记在这里。
   */
  tabPairSplitMs: 351,
  /**
   * **二合一 / 拆开**单次允许出现的强制排版次数上限(⑤b 真正的红绿线)。
   *
   * 整数计数,不随机器噪声抖。首次读数 10 下**逐字相同**:`3 3 3 3 3 3 3 3 3 3`,
   * 所以取 3、不留余量(与 `sessionSwitchForcedLayouts` 同一条立法理由)。三次各是谁:
   *  1. React 提交前后那一次选区/焦点快照(逐个祖先读 `scrollTop`),0.7–1.4ms;
   *  2. `useLeafGeometry` 的每渲染重量(顶栏标签组要坐在叶正上方),0.5ms;
   *  3. `ChatStream` 的贴底 —— 那一格地变窄了一半,这次排版就是这一下的正事,150–164ms。
   * 哪天内容层跟着标签的身份走(两块内容当场卸载重挂:聊天区重跑一次进场、查看器丢掉
   * 滚动位),这一格会多出好几次,当场变红。
   */
  tabPairSplitForcedLayouts: 3,
  /**
   * **条内换序一整趟(按下 → 12 发 move → 松手 → 稳定)的时长上限**
   * (W6-b,设计 `docs/workbench-tabs-2026-09.md` §4.5 第 1 条「1:1 跟手」)。
   *
   * 换序这一下**语义上什么都没变**(树到落定那一刻才动),屏幕上动的只有一格
   * `transform` —— 所以它理应几乎不花钱。而「理应」正是要量的东西:哪天有人把
   * 每帧的 `track()` 接回 React、或者让邻居的中线去读活矩形(每帧一次强制排版),
   * 这一格当场翻倍。
   *
   * 时间那一半只当参考(它随机器抖),红绿的主判据是下面那格强制排版次数 ——
   * 与 `sessionSwitchMs` / `tabPairSplitMs` 逐字同一条立法理由。
   *
   * **一趟的地板不归产品管**:12 发 move **各让出一帧**是量法本身要的(不让的话 12 发
   * 被合成一个任务,量出来的是「一次批处理」),所以一趟至少要 12 个刷新周期 ≈ 90ms
   * @60Hz。预算因此不是「一趟该多快」,而是「地板之外还剩多少」。
   *
   * **读数(2026-09-05,本机)**:W6-b 交卷时 `259 501 210 448 …`、p95 **494ms**
   * (逐趟跳一倍是当时那个病:一半的趟数里 React 把装着整块内容的画法层搬了家);
   * W6-p 修完 `110 114 106 109 …`、p95 **114ms**,20 趟里最大 115、最小 105,
   * **一个跳变都没有**。预算 = 115 × 一成五 ≈ 132,取 160 给刷新率不同的机器留一格
   * (120Hz 上地板会掉到 ~50ms,而慢机器上会长)—— 它仍旧接得住真回归:那 ~230ms
   * 的尾巴一回来,一趟直接 300+。
   */
  tabReorderMs: 160,
  /**
   * **松手 → 稳定**那一段的 p95 上限,毫秒(W6-p 新增,直接对着 W6-b 的留账)。
   *
   * 一趟里只有这一段是**产品说了算**的:落定(`moveTab` + 开闸 + 播报)与收笔
   * (FLIP + 闪圈)。前面那 12 帧是量法的地板,把两者混在一个数里,W6-b 留账的
   * 「真换序比不换序多 ~230ms」就永远说不清是谁的。
   *
   * 读数:改前 65ms(便宜的那半趟)/ **285ms**(贵的那半趟);改后
   * `18 16 16 17 17 17 18 16 17 …`,p95 **18ms**、最大 20ms —— 跳变整个消失了。
   * 预算 60ms:这一格自己也有地板(门在松手之后等**两拍 rAF** 才落 `:end`,
   * 那就是一到两帧),60 = 那个地板 + 落定那一段 + 噪声,而 200ms 级的回归当场拦下。
   */
  tabReorderTailMs: 60,
  /**
   * **一整趟换序允许出现的强制排版次数上限**(⑤c 真正的红绿线)。
   *
   * 一趟里有 12 发 `pointermove`。基准矩形那条判词说的正是这件事:**整场只量一次**
   * (`lift()` 那一下),之后每帧只写 `transform`。所以这个数与 move 的发数**无关**
   * —— 它要是随发数线性长,就说明有人在每帧问 DOM「你现在在哪儿」,而那正是
   * §4.5 第 1 条要杀的东西。
   *
   * **6 是这份代码今天的确定性读数**(20 趟逐趟都是 6,一次不抖;逐次 0.4–0.99ms,
   * W6-b 改前其中两次是 53.8ms 与 41.4ms)。六次各是谁(名字取自不压缩那一版
   * `vite build --mode development --minify false` 的 trace 顶帧,不是猜的):
   *  1. `DragSession.band()` → `rect()` 问这条条此刻的矩形(起拖那一帧);
   *  2. React 的 `commitBeforeMutationEffects` —— 落定那次提交前的读;
   *  3. `useTabDrag` 收笔的「先量后改」(`liveLeftOf`:滑入那一程要它此刻在手上的左缘);
   *  4. 同一只 `commitBeforeMutationEffects` 的第二次(内容分两次提交);
   *  5. **`ui/Tabs` 的 `clippedTabIds`**(`measure` 在一只 layout effect 里跑)——
   *     「条上有几格没露全」只有条自己量得出,判词在 `TabsOverflow` 上;
   *  6. `tabStripChoreo.settle()` 的 FLIP(排一帧之后读新槽位,再写起点)。
   * 六次都是**每趟固定一次**,与 12 发 move 无关 —— 这正是这一格要证的那句话。
   *
   * ── 09-06(W7-c)从 5 改成 6:第 6 次是谁,以及它是哪一批的 ──────────────────
   * W6-p 把这一格标到 5 时,第 4 位记的是 `useLeafGeometry`(W1-b 的顶栏几何)。
   * **那一次随 W7-c 裁定 1 一起没了**(`leaf-geometry.ts` 删除),所以它不在上面
   * 那张表里;顶上来的是 **W7-t 加的 `clippedTabIds`** —— 那一批只跑了
   * gate:focus / gate:squeeze / gate:a11y,没有回过 gate:perf,于是这一格在合树时
   * 静静地从 5 变成了 6,W7-c 跑门时才第一次显形。
   * 抬这一格不是「读不到 5 了就抬」:它有名有姓、是一件**正当**的量(条上有几格
   * 没露全,只有条自己答得出),而且确定性(20 趟不抖)、便宜(0.6–0.7ms)——
   * 与 W6-p 当年 3→4 的判法逐字同一条。两条时间线仍有余量:一趟 p95 109ms ≤ 160,
   * 松手→稳定 p95 17ms ≤ 60。
   *
   * **反证跑过一次并被推翻**:先猜第 6 次是「裁定 1 把顶栏那一组改回普通 flex 项、
   * 于是条里插一格空位会弄脏顶栏的排版」,给 `.group` 加 `contain: layout` 复量 ——
   * 读数**照旧是 6**,猜错了,那条 CSS 当场撤掉(不留一条说不出理由的规则)。
   */
  tabReorderForcedLayouts: 6,
  /** 长帧(LoAF)的定义线,毫秒。探针也用这个值当订阅阈值。 */
  longFrameMs: 50,
  /** 动画期间允许出现的长帧条数 —— 零。 */
  animationLongFrames: 0,
  /** 流式稳态下允许出现的「超过这个毫秒数」的帧,条数上限见 streamOverBudgetFrames。 */
  streamFrameMs: 33,
  /** 流式稳态允许的超标帧条数。 */
  streamOverBudgetFrames: 0,
  /** event timing 的订阅阈值:低于它的交互不进环,省得把环冲满。 */
  eventDurationThresholdMs: 40,
} as const

export type PerfBudget = typeof PERF_BUDGET

/** 一条读数是否超预算。HUD 的红/绿与门的红/绿走的是同一句判断。 */
export function overBudget(kind: 'longFrame' | 'interaction', ms: number): boolean {
  return kind === 'longFrame' ? ms > PERF_BUDGET.longFrameMs : ms > PERF_BUDGET.interactionP95Ms
}
