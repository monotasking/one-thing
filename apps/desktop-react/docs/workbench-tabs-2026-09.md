# 标签条与拖拽 v3：一条标签条、一个标签最多两格、每一步都有回应

2026-09-05。接 `workbench-2026-09.md`（拼贴台 v2 正本）。v2 的八批已全部合入 main（W1-a 7347cb52 … W5-b eec30723），用户在真机上连报三轮：

- 拖一条会话到顶栏的标签上，不知道能不能放，没有任何交互、手感、提示；
- 拖不到聊天区；文件还是只能打开一个标签；
- 「拖拽 session 的 tab 本就是个伪需求，我最多就是一个分屏，往一个 tab 有两个（二合一）的方向设计」；
- 「files 本身应该是一个可以打开多个的存在，例如 note、例如 workdir，名字应该和 viewer 差不多，设置为目录名」；
- 「手感很差，tab 为什么拿不出来？点击和拖拽都分不清」；「好好设计一下 tab header 上的拖拽、排序」；
- 「不知道为什么 tab 背景颜色变了，拖拽没有浏览器的 tab 的丝滑、流畅和交互感」。

这份文档把这些话收成一个模型、一套手势、一张反馈表和三批施工。它**推翻 v2 在中央区的两条决定**：任意分屏树不再暴露给用户，顶栏不再按叶分组画多条标签条。架子与浮窗里的树照旧（W4 的结构不动），但它们的檐也只暴露标签与二合一，不再暴露「分屏 ▸」菜单。

---

## 1. 借谁的手感

成熟的拖拽体验不是发明出来的，是从用户已经练过的肌肉记忆里借来的。本方案每一条手势都能指出出处：

| 手势 | 出处 | 我们的用法 |
| --- | --- | --- |
| 按下标签立刻切换，不等松手 | Chrome、Safari、Finder 的标签 | 按下即选中，松手不动就是一次点击 |
| 横向拖动很小的距离就开始换序，被拖的标签跟手、邻居滑开让位、松手滑入槽位 | Chrome 标签条 | 横向 6px 起拖；邻居 120ms 让位；松手 150ms 滑入 |
| 被拖的标签**长得和原来一模一样**，只是浮在邻居上面；其它东西一律不变色 | Chrome 标签条 | 被拖标签不换底色、不放大、不改形，只加一层很轻的投影 |
| 竖向拉出标签条一段距离才「撕」下来 | Chrome 拖出新窗口 | 竖向离开标签条 24px 撕下成卡片 |
| 把一个标签放到另一个标签上 = 两个并排 | Arc 的 Split View、Safari 的分屏 | 二合一 |
| 拖到项目**上**与拖到项目**之间**是两件事，由位置决定 | Finder 列表里拖进文件夹 vs 插到两项之间 | 标签往下压 6–24px 放到另一个标签上 = 二合一；在条里横着走 = 换序 |
| 落点在内容区上显示「落下后占的那一半」 | VS Code 编辑器拖放 | 内容区右侧 28% 带亮出右半边 |
| 拖着的东西下面挂一行说明 | macOS 拖放的角标（＋ / ✕）、Figma 的拖拽提示 | 浮影下常驻一行「松手会发生什么」 |
| 不能放的地方光标变禁止、松手弹回 | macOS / Windows 通用 | 拒绝态 |

---

## 2. 模型：中央区只有一条标签条，一个标签最多两格

### 2.1 数据结构

中央区不再是任意深度的拼贴树。它是一条标签列表，每个标签装一格或两格内容：

```ts
/** 一格内容，与 v2 的 ContentRef 完全相同：种类 + 键。 */
interface ContentRef { kind: string; key: string }

/** 一个标签。panes 长度为 1 或 2，永远不会更多。 */
interface Tab {
  id: string
  panes: [ContentRef] | [ContentRef, ContentRef]
  /** 两格时左格占的宽度比例，0.2–0.8，缺省 0.5。拖分隔杆改它。 */
  ratio: number
}

/** 中央区。 */
interface CenterStrip {
  tabs: Tab[]
  /** 活动标签的下标。 */
  active: number
}
```

它与 v2 树的关系是：**中央区的树退化成一片叶**，叶里每一格 tab 就是上面的 `Tab`。两格的标签在树上是一个 `row` 切分节点带一个比例；一格的标签就是一个 `ContentRef`。也就是说，v2 的 `PaneNode` 类型不删，只是中央区永远只允许深度 1，`splitLeaf` 在中央区没有调用者。架子与浮窗里的树也按同一条政策渲染：一片叶 = 一条标签条，标签里至多两格。

为了让「两格」在登记表里也是一种内容而不是特例，加一种内容种类 `pair`：

```ts
/** kind = 'pair'，key = `${refId(a)}|${refId(b)}`。 */
registerContentKind({
  id: 'pair',
  title: (ref) => ({ text: `${titleOf(a)} ⫽ ${titleOf(b)}` }),
  icon: (ref) => iconOf(a),
  render: (ref) => <PairPane a={a} b={b} />,   // 两格各自 renderRef，中间一根 ui/Splitter
  beforeClose: (ref) => 两格各问一次，任一格说「别关」就不关,
  dispose: (ref) => 两格各 dispose,
})
```

这样 `workbench/*` 里一个字都不用出现「两格」这个概念：树上它就是一格 tab，标签条画它的标题，关它就是关它。「拆开」是把一格 `pair` 换成两格普通 tab；「二合一」是把两格普通 tab 换成一格 `pair`。两个动作都是 `tree.replaceRef` / `insertTab` / `removeTab` 的组合，不需要新的树操作。

### 2.2 为什么是两格不是树

用户原话是「我最多就是一个分屏」。任意树带来的三个代价——顶栏要按叶分组画多条标签条、每片叶各有一组动作、落点要判五个方向——在两格模型里全部消失：顶栏只有一条标签条；动作组只有一组；落点只剩「并成新标签」「与它并排」两种。VS Code 支持任意编辑器组，但绝大多数人只用到左右两个；Arc 的 Split View 最多四格，常用的也是两格。两格是够用的上限。

### 2.3 三条不变量

1. **一个内容在中央区只出现一次**。把已经开着的文件拖到别处是搬，不是复制（v2 已是如此）。
2. **会话标签至少有一个，且不可关**（v2 的 `resident` 契约不变）。
3. **两格标签里两格不同**。把 A 拖到「A ⫽ B」上是空动作，不是替换。

---

## 3. 文件面板按目录多开

今天 Dock 上一块「Files」瓦，只会打开工作目录，名字永远叫 Files。改成：

- **每个目录是一份面板内容**：`files-root:<绝对路径>`（W3 已登记这一种，本方案把它扶正为文件面板的唯一形态，`panel:files` 退役）。标签与瓦上的名字是目录名（`basename`），Tooltip 是全路径；两个目录同名时名字后面带上父目录（`docs · a` / `docs · b`），与查看器对同名文件的做法一致。
- **Dock 上那块瓦改成「目录」**：点它打开当前会话的工作目录那份面板；右键菜单列出最近打开过的目录和「打开目录…」（系统对话框）。打开过的目录记在 workbench store 里按工作区持久化，最多记 20 个。
- **一个目录面板可以住在任何地方**：左架子、浮窗、中央区的一个标签，都行。两个目录面板并排是常态：左架子那条标签条上就是 `start-electron | notebook`。
- **文件单击 = 开一个标签**（在焦点标签条的末尾），已经开着就切过去并让那个标签闪一圈。预览标签整体退役：`Tab.preview` 字段、斜体标题、单击顶替、↵ 固定这四样一起删。理由：预览是 VS Code 为「快速翻看很多文件」设的，代价是用户搞不清为什么只有一个标签；这次用户明确说了他要的是多个标签。真机复现还抓到 `tree.insertTab` 对已开着的 ref 提前返回、`preview:false` 永远清不掉的 bug，随退役一起消失。

---

## 4. 手势：一张状态机

### 4.1 阈值

| 常数 | 值 | 为什么 |
| --- | --- | --- |
| `DRAG_START_X` | 6px | 标签横向起拖。比 W3-b 的 8 小一点：换序是标签最常见的拖法，起得要快；但要比触控板的点击抖动（2–3px）大。 |
| `DRAG_START` | 8px | 行、瓦等来源的起拖，任意方向。 |
| `TEAR_OFF` | 24px | 竖向离开标签条多远算撕下。等于标签条高度的三分之二，与 v2 的 `TEAR_OFF_DISTANCE` 同值。 |
| `ONTO_FROM` | 6px | 指针低于标签条底缘多少算进入「放到标签上」带（到 `TEAR_OFF` 为止）。第一版用的是停驻 300ms，真机上换序途中稍一停顿就误并，改成位置判据。 |
| `NEIGHBOR_MS` | 120ms | 邻居让位的过渡。 |
| `SETTLE_MS` | 150ms | 松手滑入槽位。 |
| `PAIR_BAND` | 28% | 内容区左右各多宽算「与它并排」。 |
| `TAB_MIDDLE` | 44% | 标签正中多宽算「落到它上面」，两侧各 28% 算「落到它旁边」。 |

### 4.2 标签上的手势

```
idle ──按下──▶ pressed（当场激活这个标签；光标 grabbing；标签本身不动、不变色）
pressed ──松手，位移 < 6──▶ idle（一次点击，到此为止）
pressed ──|Δx| ≥ 6──▶ reorder
pressed ──|Δy| ≥ 24──▶ tear

reorder：被拖标签浮到邻居上面（只加一层很轻的投影，不放大、不改形、不换底色），
         x 零延迟跟手，夹在标签条两端之内；
         邻居让位的判据是**被拖标签朝运动方向的那条边越过邻居中心**（Chrome 的规则）：
         往右拖看被拖标签的右边缘，右边的邻居中心被它越过就滑到左边去；往左拖看左边缘。
         不是中心对中心——中心对中心时宽标签要整个越过窄邻居才换位，手感发黏，而且永远够不到末位；
         也不是把邻居按「拿走被拖那格之后」的位置算——那样宽标签一抬起来右邻居就先跳一下。
         邻居中心用抬起那一刻量好的位置算，不读正在过渡中的活矩形，否则拖快了槽位会漂。
         最终下标 = 左边仍未被越过的邻居数 + 右边已被越过的邻居数，不再另外加减；
         标签条与内容区**什么都不变色**——浏览器换序时屏幕上只有那一格标签在动。
reorder ──指针压到标签条底缘下 6–24px──▶ onto（邻居不再让位；指针底下那个标签描圈亮起；浮影只剩一行「与 X 二合一」）
onto ──回到条内──▶ reorder
reorder ──竖向离开标签条 ≥ 24──▶ tear（原位折成 0 宽、邻居合拢；标签变成一张卡片跟手）
tear ──回到标签条带内──▶ reorder（原位展开，继续换序）
reorder ──松手──▶ moveTab；被拖标签从手上的位置 150ms 滑进新槽，落定闪一圈
onto ──松手──▶ 二合一（被拖标签并进目标标签的右格；目标已是两格则替换右格；指针底下没有标签 = 滑回）
tear ──松手──▶ 按 §5 的落点表落定；落在自己那格的内容区 = 放回
任何态 ──Esc / pointercancel / 窗口失焦──▶ 滑回原位（150ms），树一字不变
```

「按下即激活」是 Chrome 的做法，也是用户「点击和拖拽分不清」的解药：按下那一刻反馈已经给了（内容切过去了），后面要么松手结束，要么拖走。实现上它要求**标签条 DOM 在按下时不能重建**——只换活动态属性和内容区，被按住的元素必须还在手里。样例第一版就是在这里失手的。

### 4.3 行、瓦上的手势

```
idle ──按下──▶ pressed（抬起 1px；光标 grabbing）
pressed ──松手，位移 < 8──▶ 打开（文件：末尾开新标签或切过去；会话：焦点标签的会话格换成它；目录：打开目录面板）
pressed ──位移 ≥ 8──▶ drag（浮影卡片：图标 + 名；行本身留在列表里变淡）
drag ──移动──▶ 按 §5 的落点表实时判定并显示
drag ──松手──▶ 落定；卡片 180ms 飞进空位/槽位；落定的标签闪一圈
drag ──松手在拒绝态──▶ 卡片弹回来源行
```

### 4.4 触控板与鼠标

两者走同一套 pointer 事件。触控板上「按住拖动」是三指或按住不放，阈值不变。双击不做任何事（本仓判例：双击与右键在触控板上打架）。

### 4.5 流畅的三条硬规矩

浏览器标签拖起来「丝滑」不是动画多，是三件事都做对了：

1. **被拖的东西 1:1 跟手**：每一个 `pointermove` 直接写 `transform`，不经 React、不节流、不加缓动；被拖标签的过渡关掉，`will-change: transform` 提前声明。
2. **只有该动的在动**：邻居用 120ms 过渡滑开；标签条、内容区、其它标签的颜色一律不变。所谓「起拖后可放的地方淡亮」只用于**从外面**（会话行、文件行、Dock 瓦）拖进来的情形，换序时不做。
3. **反馈是同一个节点在挪**：空位是同一格只在落点变化时换位置，圈和半边是同一个覆盖层换矩形；任何按帧拆掉重建的反馈都会被看成闪烁。

---

## 5. 落点与反馈：一张表

拖拽期间**先判后画**：每一帧算出唯一的落点，再按表画反馈。判定次序从上到下，先命中先赢。

| 落点 | 判定 | 屏幕上 | 浮影下那行字 | 松手 |
| --- | --- | --- | --- | --- |
| 拒绝区 | 指针在红绿灯、顶栏尾格、Dock 上 | 卡片变灰、虚线边；光标 not-allowed | ✕ 这里不能放 | 弹回 |
| 标签正中 | 指针在某个标签中间 44%（被拖标签自己除外） | 那个标签描圈、底色亮 | 与「X」二合一 / 替换「X 的右格」 | 并成两格 |
| 标签之间 | 指针在标签条带内，不在任何标签正中 | 那个位置腾出一格带边的空位（宽 = 卡片宽）；悬到的标签让开 | 放到第 n 位 | 插入 |
| 窗口边带 | 指针在窗口四边 24px 内 | 那条边亮一层薄膜 | 钉到左侧架子 | 进架子 |
| 内容区右带 | 指针在活动标签内容区右侧 28% | 右半边亮出来；两格时写「替换 X」 | 与「X」二合一 · 放在右侧 | 并成两格 |
| 内容区左带 | 指针在左侧 28%，且活动标签是一格 | 左半边亮出来 | 与「X」二合一 · 放在左侧 | 并成两格，它在左 |
| 内容区中间 | 其余 | 内容区描一圈；标签条末尾腾空位 | 开成新标签 | 末尾开新标签 |
| 自己的内容区 | 拖的是活动标签本身 | 不画 | 松手放回标签条 | 放回 |
| 窗外 | 指针出了窗口 | 卡片变成一张虚线窗轮廓 | 松手成新浮窗 | 新浮窗 |

两条贯穿所有落点的规则：

1. **从外面拖东西进来时**，起拖那一刻所有能放的地方先淡淡亮一层（标签条、内容区、四条边带各铺一层 6% 的主题色），悬到的那一处再亮到实。换序不适用（§4.5）。
2. **浮影下那行字永远不空**。落点变了字就变，没有落点写「松手放回」。这是唯一一处在拖拽期间出现文字的地方，内容区上不再写字（v2 的色块加文字太重）。

**这两条只对「外来来源」成立，条内换序豁免**（W7-c 裁定 5，B10）。条内换序（拖一格标签在它自己那条条上换位置）屏幕上只有两件事：**被拖的那格抬起来**、**邻居让位**——不画浮影、不画空位、不画那行字。理由是它压根不需要预示：那一格自己就在手上，它挪到哪儿眼睛直接看得见；再叠一张浮影与一格空位，屏幕上会同时有三个「它」（原位的、浮影的、空位的）。W6-b 起代码就是这么做的，这一节的字面到 W7-c 才补上——**v2 §323 不动**（那是外来来源那一档的正本）。

反馈都是同一个 DOM 节点在挪，不是拆掉重建：空位是同一格只在落点变化时换位置，圈和半边是同一个覆盖层换矩形。样例第二版「标签条上没手感」的病根就是空位每帧重建、宽度动画永远从 0 起。

---

## 6. 二合一的细节

- **哪一格是焦点**：点哪格哪格拿焦点；输入框跟着有会话的那一格（路线 B，W5-b 已如此）。两格都是会话时跟最近点过的那格。
- **标签的标题**：`A ⫽ B`，两个图标各自画，中间一根 1px 竖线；最大宽度 260px，长了各自截断。
- **格头**：每格顶上一条 28px 的小檐，写这格的名字和一颗「拆开」；一格标签没有格头（与今天一样）。
- **拆开**：右格拆出去成为紧邻其后的新标签，左格留在原标签里，焦点留在原标签。
- **关闭**：关一格标签 = 两格各问一次 `beforeClose`（脏文件确认），任一格拒绝就不关。关格头上的 ✕ 只关这一格，另一格变回一格标签。
- **分隔杆**：`ui/Splitter`，比例存在标签上，键盘 ←/→ 5% 一步。
- **持久化**：`pair` 就是一格普通 ref，随中央区标签列表一起进 `onething.workbench`。`session:new` 那格（W5-b 的保留键）也能参与二合一。
- **不允许**：三格；`pair` 里再放 `pair`（拖一格两格标签到另一个标签正中 = 拒绝，理由「两格的标签不能再并」）。

---

## 7. 键盘与无障碍

每一个拖拽能到的落点都有一条不用鼠标的路，动作单产地是标签的右键菜单（本仓判例）。

### 7.1 那张表只有六项（W7-c 裁定 3）

用户 09-05 的原话是「按钮太多、菜单太多」。表收成六项：

| # | 项 | 在场判据 |
| --- | --- | --- |
| 1 | 与右边的标签二合一 | 右边还有一格，且两边都不是两格标签（否则禁灰） |
| 2 | 与左边的标签二合一 | 同上，往左 |
| 3 | 拆开 | **只在两格标签上出现**，不是灰掉——普通标签上它没有对象 |
| 4 | 撕成浮窗 | 恒在（空叶时禁灰） |
| 5 | 移到架子 ▸ | 子菜单四行：左侧栏 / 右侧栏 / 顶栏 / 底栏 |
| 6 | 关闭 | 走 `useCloseLeafTab`（与 tab 上那颗 ✕、与 ⌘W 同一只） |

- **架子叶与浮窗叶多一节「分屏 ▸」**（在右侧 / 在左侧 / 在下方 / 在上方）——中央区收成一条标签条之后那四项在那里没有落点（§2.1）。
- **架子叶再多两行**（弹出为浮窗 / 关闭整栏）、**浮窗叶再多两行**（钉到边 ▸ / 上舞台）——它们从檐上的钮搬进来（裁定 4，见 §7.3）。
- **项名必须带宾语，不许裸方位词**（B5）：折成子菜单之后动词落在父行上，子行说的是宾语。
- **「左移一位 / 右移一位」从表里删掉**：换序靠拖拽（§4.2 的判据比按两下菜单快得多），键盘等价升格成**全局命令**（下面 7.2）。

### 7.2 两个开口，一张表

| 开口 | 说明 |
| --- | --- |
| 右键一格标签 | 光标那一点 |
| `Shift+F10` / 上下文菜单键 | 焦点标签的左下角。**删掉那颗钮之后这是唯一的键盘入口** |
| 右键架子 / 浮窗那条檐的空白处 | 同一张表（那两处的宿主行就在里面） |

「开不开、开在哪」这一格事实只有一个产地（`workbench/leaf-menu.ts`）；判据、项目、动作整件在 `LeafActions` 里。**中央区没有第三个开口**——那颗「分屏」钮 W7-c 删了。

### 7.3 拖拽 ⇄ 键盘的等价表

| 鼠标 | 键盘 / 菜单 |
| --- | --- |
| 换序 | **全局命令** `workbench.moveTabLeft` / `moveTabRight`（出厂 `⌘⌥⇧←` / `⌘⌥⇧→`）。落定走的仍是拖拽落定同一只 `drop-commit.reorderTab`。标签条内 ←/→ 只移焦点不换序（APG 手动激活档）<br>**为什么不是规格点名的 `⌘⌥←/→`**：那两个位子从 09-01 起是左 / 右架子的收展（「方向即语义」那一族）。加一个 ⇧ = 「带着这一格一起走」，跨应用惯例（VS Code 的 ⌥⇧↑↓ 搬一行）；全表零冲突 |
| 二合一 | 菜单「与右边的标签二合一」「与左边的标签二合一」 |
| 拆开 | 菜单「拆开」；缝中点那颗小把手 |
| 撕成浮窗 / 钉到架子 | 菜单「撕成浮窗」「移到架子 ▸」。**新窗的矩形**(640×480、锚中央区右上内缩 24、层叠 28)只有一个产地 `transitions.freshFloatRect` —— W7-p 修一轮裁定 1,正本在 `workbench-2026-09.md` §2.1 |
| 关这一格 | 菜单「关闭」；tab 上那颗 ✕；⌘W |
| 打开文件为新标签 | 文件树里 ↵ |

播报（`announce()`）：换序「已移到第 n 位，共 m 位」；二合一「已与 X 并排」；拆开「已拆开」；搬到边上「已移到右侧」；撕成浮窗「已撕成浮窗」；拒绝「这里不能放」。

---

## 8. 默认摆法

真机复现出来的「拖不到聊天区」不是拖拽判据的错，是文件面板出厂摆法是一扇浮窗，正好停在聊天区中间，往那一放落进的是浮窗自己。改成：

- 目录面板出厂摆法 = 左架子；会话总览出厂摆法 = 左架子。
- 浮窗仍然是合法落点，但不再是任何面板的出厂档。
- 从浮窗里拖东西出去时，浮窗自身矩形不再是落点（拖的东西来自它，它不该接住自己），指针在窗内时按窗底下的那片叶判。

---

## 9. 与今天代码的落差

| 今天 | 本方案 | 落点 |
| --- | --- | --- |
| ~~中央区任意分屏树，顶栏按叶分组多条标签条~~ | 中央区一片叶；顶栏一条标签条 | **W6-a 已落**：`layout.topStrips` 退成单条；`LeafActions` 的分屏四项在 `SINGLE_LEAF_REGIONS` 里整组不画（禁灰说的是「此刻不行」，而那里是「这个区域不存在这件事」）|
| ~~没有「两格」概念~~ | `pair` 内容种类 | **W6-a 已落**：`content/kinds/pair.tsx` + `pair-ref.ts`；核心层零枚举——判「是不是两格」问的是 `ContentKind.composite`，`workbench/*` 里 grep `pair` 零命中 |
| ~~单击 = 预览 tab 顶替；↵ 固定失效（bug）~~ | 单击 = 开新标签；预览退役 | **W6-a 已落**：`tree.ts` 的 `preview` 整档删除，`insertTab` 那个提前返回一并修掉 |
| ~~文件面板出厂档 `panel`，根本不进标签条~~ | 出厂档 = 主区域新标签 | **W6-a 已落**：`FACTORY_FILE_OPEN_MODE = 'stage'`；`gate:files` 第 8 步抹掉那格偏好再整页重载，按**出厂值**验「连点三个文件三个标签」|
| ~~一块 Files 瓦只开工作目录~~ | 目录面板按目录多开，名字 = 目录名 | **W6-a 已落**：`content/kinds/files-root.tsx` 扶正、瓦改「目录」、`recentRoots` 表。「打开目录…」今天是**一个路径输入框**（结构化降级，判词整段在 `content/files/open-dir-hub.ts`；原生对话框见 §12 留账）——**W6-c** 的 `gate:files` 补了这一档：右键瓦 → 点那一项 → 敲一条磁盘上真在的路径 ↵ → 架子上真的多出一格 |
| ~~面板出厂摆法是浮窗~~ | 左架子 | **W6-a 已落**：`stage/items.ts` 的 `defaultPlacement` |
| ~~腾空位只有 tab 来源做~~ | 五种来源统一 | **W6-b 已落**：空位 / 描圈 / 半边 / 边带 / 轮廓 / 氛围 / 提示行全部由 `DragSession` + `DropOverlay` + `ui/tab-reorder` 驱动，来源只交 ref 与几何 |
| ~~浮影只有名字~~ | 名字 + 一行说明 | **W6-b 已落**：`DragGhost` 的 `hint` 槽（i18n `drag.hint.*`，永不空）；落区上从此一个字都不写（`DropFeedback.label` 退役） |
| ~~内容区并入 = 整块描圈，分屏 = 16px 边带一根杠~~ | 中间 = 新标签，左右 28% = 二合一 | **W6-b 已落**：`drop.ts` 判定表按 §5 九行重写；`DropZone`/`zoneAt`/`zoneRectOf`/`ZONE_SPLIT`/`DROP_EDGE_PX`/`DROP_BAR_PX` 与 `{kind:'leaf'}` 整支删除，新增 `half` 画法与 `pairRectOf` |
| ~~换序松手瞬间换树~~ | 150ms 滑入 | **W6-b 已落**：`tab-reorder.settle()`（FLIP，`--dur-settle`）+ 落定闪一圈（`--kf-tab-land`）；「放到标签上」松手在空白处也走它（树不变，那一格滑回原位） |
| ~~换序邻居中点读活矩形~~ | 读抬起时的基准矩形 | **W6-b 已落**：`lift()` 量一次基准，`track()` / `hover()` 全程只读它（落点与让位同一句话算出来） |
| ~~换序判据是中心对中心~~ | 被拖标签朝运动方向的那条边越过邻居中心 | **W6-b 二修已落**：`track()` 里 `i < index ? left >= c : left + w > c`，最终下标 = 计入的个数，**两端不再有钳位特例**（那两句是错判据的症状，随它一起删）。单测在 `ui/__tests__/tabs-joined.test.tsx`（门槛表 + 宽标签够得到末位 + 刚抬起邻居不动）与 `workbench/__tests__/drop-commit.test.ts`（`at` 落到树上的五种结果）|
| ~~抬起的标签放大、换底色、改形~~ | 原样浮起，只加轻投影 | **W6-b 已落**：`[data-lift]` 只写 z / `--drag-lift-shadow` / `transition:none`；条上 `[data-reorder]` 把 hover 底整条关掉（§4.5 第 2 条的结构保证） |
| ~~无「放到标签上」~~ | 往下压 6–24px 放到标签上 = 二合一 | **W6-b 二修已落**：带的上沿是 `ONTO_FROM_PX` 6（`ui/drag/constants.ts`），下沿就是 `bandSlack` = `TEAR_OFF_DISTANCE` 24（**不写第二个 24**）；模式住在 `useTabDrag` 的一格 ref，只在跃迁那一刻切呈现与描圈；编舞那一头是 `tab-reorder.hover(x)`（跟手照旧、邻居让位清零、按**整宽**答指针底下是谁）。第一版那套按时间判的门槛整件退役（连它的 `--dur-*` token 一起）|
| ~~按下不激活，click 才激活~~ | 按下即激活 | **W6-b 已落**：`LeafStrip` 的 `onTabDown` 先 `select(id)` 再起手势；`ui/Tabs` 的 key 稳定，按下前后是同一个 DOM 节点 |
| ~~换序不保预览身份（W3-b 留账）~~ | 预览退役后无此事 | **W6-a 已了结**：预览整档退役，这笔账没有对象了 |
| ~~拆开只有菜单说话，格头那颗钮一声不吭~~ | 两条路同一只动作、同一句播报 | **W6-c 已落**：`drop-commit.unpairTab` 是拆开的唯一产地（播报在它里面），`LeafActions` 与 `content/kinds/pair.tsx` 两处消费它 |
| ~~那张动作表只有檐上那颗钮一个开口~~ | 右键一格标签开**同一张**表 | **W6-c 已落**：`ui/Tabs.onTabContextMenu` + `workbench/leaf-menu.ts`（「开不开、开在哪一片叶、开在哪个点」这一格事实的唯一产地）。判据、项目、动作一格没搬，仍整件在 `LeafActions` 里 |
| ~~拒绝松手时一声不吭~~ | 播报那句理由 | **W6-c 已落**：`ui/drag/DragSession` 的 `up` 里念 `drop.hint`——它站在所有来源与两条落定路的**上游**（条内换序那一形的拒绝由 `inline.drop` 当场吃掉，一步都不经过 `dropRef`）|

**§9 落差表到此全部勾掉。**

不动的：架子与浮窗的树结构（W4）、`stage/placement.ts` 的住处写口、真全屏（W2）、会话多开的投影与清洗（W5-b）、聊天跟随与工具卡（C1/C2）、响应链。

---

## 10. 施工分期

三批，每批照旧带状态先行三表、零重挂断言、`ui:consume`、真机门、反证。

**W6-a 模型收口（先做，改结构不改手感）**
中央区收成一片叶（迁移：多叶存档按阅读序并成一条标签列表，比例丢弃，`persist-migrate` v3）；`pair` 种类 + 拆开 / 二合一两个 store 动作 + 菜单项；预览退役（含 `insertTab` 那个 bug）；文件面板按目录多开 + 瓦改「目录」+ 最近目录表；出厂档改主区域新标签、出厂摆法改左架子；顶栏退成单条。验收：gate:files 五档照旧 + 「连点三个文件三个标签」；gate:focus 在单条标签条上照跑；新增 pair 的零重挂断言（并、拆、换比例三步内容根节点不换）。

**W6-b 手感（拖拽引擎重做）—— 已落地（2026-09-05；二修同日）**
按 §4 状态机重写 `ui/tab-reorder.ts` 与 `useTabDrag`（按下即激活、6px 起拖、夹在两端、基准矩形、150ms 滑入、往下压放到标签上 = 二合一、撕下与拉回、Esc 滑回、§4.5 三条硬规矩）；`DragSession` 统一驱动五种来源的反馈（空位、描圈、半边、边带、轮廓、拒绝），来源只交 ref；`DragGhost` 加提示行；从外面拖入时的氛围淡亮；拒绝态光标；落定飞入与闪圈。验收：`gate:drag` 整套改写，**所有场景用连续不停顿的指针序列量**（这次的教训：停住再量抓不到「一直在动就没反应」），逐条断言表里每一格的屏幕读数（空位宽度 > 0、圈的矩形、半边的矩形、提示文本、光标）；换序场景断言活矩形过渡中槽位不漂、换序期间标签条与其它标签的底色逐字不变；「换序途中停住 1s 不误并」场景用真等待。

落地补记（二修，把第一版的两处旧规格改到位）：

- **换序判据**从「中心对中心 + 两句两端钳位特例」改成「边越过中心」（§4.2）。旧判据在真机上的两种长相都被单测钉住了：宽标签越不过窄邻居、以及末位/首位够不着而只能靠特例救。`gate:drag` 场景 ③ 追加三条——刚抬起那一帧邻居一个都不动、最宽的那一格拖得到末位、以及**逐个右邻居**在「被拖右缘 = 它中心」两侧各 3px 处读一次 transform（中心对中心在「已越过」那一站当场红）。
- **二合一**从「在邻居正中停住 300ms」（时间判据）改成「指针压到标签条底缘下 6–24px」（位置判据，§4.1 `ONTO_FROM`）。旧那一套的三件——时长门槛常量、手抖上界常量、以及它的 `--dur-*` token 与 JS 镜像——整件退役，`ui/drag/constants.ts` 换上 `ONTO_FROM_PX`。`gate:drag` 场景 ④ 整个改写成六格：横拖停 1s **不误并**（这一条就是用户报的那一下的反证）／压进带里描圈 + 提示行 + 卡片不画 + 让位全清零／回条内回换序／松手并成两格（`data-tab-slots = 2`）／压在空白上是空动作／两格标签压下去是拒绝态（not-allowed + 一句理由，树不变）。

**W6-c 收尾 —— 已落地（2026-09-05）**
键盘与菜单等价、播报四句、三道门补档、v2 正本改指向。逐条：

- **拆开的唯一产地**是 `drop-commit.unpairTab`（播报在它里面），菜单与**格头上那颗钮**两处消费它。
  修前播报写在菜单项上，于是格头那颗按下去屏幕变了、**播报口一个字都没有**——同一件事两条路，
  一条说话一条不说，正是「两条路走两个动作迟早分叉」在播报上的长相。
- **拒绝那一句**（§7 播报表第四句）住在 `ui/drag/DragSession` 的 `up` 里，念的是 `drop.hint`
  ——**那句结构化拒绝的理由本身**，不是一句写死的「不行」（屏幕上写着「两格的标签不能再并」、
  读屏软件却念「这里不能放」，等于两套说法）。产地在那儿而不在落定那一头，是因为条内换序那一形
  的拒绝由 `useTabDrag` 的 `inline.drop` 当场吃掉，一步都不经过 `dropRef`——这一句必须站在
  所有来源、两条落定路的**上游**。
- **右键一格标签开出同一张动作表**：§7 首句写的是「动作单产地是标签的**右键菜单**」，而这张表
  在 W6-c 之前只有檐右端那颗钮一个开口。`ui/Tabs` 加一格 `onTabContextMenu`（与 `onTabPointerDown`
  逐字同一条纪律：Tabs 不认识「动作菜单」，只把右键连同 id 递出去），「开不开、开在哪一片叶、
  开在哪个点」这一格事实住进 `workbench/leaf-menu.ts`——它非有不可是因为顶栏那一档里标签条与
  那张表是 `TopBar` 的**两兄弟**，够不着彼此的状态。**判据、项目、动作一格没搬**，仍整件在
  `LeafActions` 里，所以两个开口开的是同一张表。菜单的无障碍名同时从「分屏」改成「标签动作」
  （表里今天装着二合一 / 拆开 / 左移右移 / 移到架子 / 撕成浮窗，中央区连分屏四项都不画了；
  屏幕上那颗钮的提示不动——它仍旧只是这张表的一个开口）。
- **平局用例扩到二合一与拆开**（`workbench/__tests__/leaf-actions-drag-parity.test.tsx`，7 条）。
  施工时当场量出一处真差别并记进用例注释：**「二合一」里谁是宿主决定两格的左右次序**，所以
  菜单「与右边的标签二合一」的等价手势是拖**右邻居到活动格上**，不是拖活动格到右邻居上——
  按后者写第一版当场红，那不是分叉，是把两个手势当成了一个。
- **三道门**：`gate:a11y` 加 `[7e]`「标签右键菜单全档可达」（右键开得出 / 与钮开出的表**逐项
  逐字相同** / 整组只占一个 Tab 位）；`gate:squeeze` 在 `checkLeafChrome` 里加「格头 28px 一行
  不换行」（高度对着 `--pair-head-h` 读，外加 `flex-wrap` 与「每件都竖着落在这条带子里」——
  只量高度会被 `height:` 钉死的那一格骗过去）与「杆推到两头窄格仍有最小格宽」（Home / End
  走杆自己的 APG 键盘路，量两格的盒 ≥ 20% 容器、格头零重叠，量完 ↵ 回缺省）；`gate:files`
  加「打开目录…」那一档（右键瓦 → 那一项 → 敲一条磁盘上真在的路径 ↵ → 架子上真多出一格，
  标签写目录名）。
- **v2 正本 `workbench-2026-09.md`** 文首多一段「v3 推翻了什么」（四行表，逐行指出被顶掉的那一节），
  §2 补上与 §3 同体例的横幅；§4 起往下仍是正本，§1 §2 §3 读作病历。

**归 W6-p（perf 收账）**：`gate:perf` 的「换序 20 次」「二合一 / 拆开 10 次」与 main 上已红的
五条一起归那一批——W6-c 一个字都不碰 `gate-perf.mjs` / `perf-budget.ts`。

### W7-c：顶栏、檐与菜单做减法（09-06，用户 09-05「按钮太多、菜单太多」）

七条裁定，逐条落地：

1. **顶栏标签从顶栏自己的开头排**。红绿灯让位右边就是第一格；`TopBarTabs` 不再消费
   `leaf-geometry`，那只文件与 `spanXVar` / `spanWVar` / `data-pane-span` 一并删除（零消费者）。
   组从绝对定位改回带子里的普通 flex 项。**`gate:perf` ⑤a 因此从 4 次强制排版收回 3**
   （多出来那一次正是 `useLeafGeometry` 的每渲染重量），判词逐条改在 `perf-budget.ts` 上。
2. **顶栏右端只留两件**：那颗 ⋯（W7-t 合成的「够不着的标签」）与 AgentChip。「分屏」那颗钮
   删除；**型工具条那一格整格退役**（`ContentKind.toolbar` 与 `FileViewerToolbar` 一起），
   它唯一的住户（markdown 的「渲染 / 源码」）搬进**内容区自己的右键菜单**，由查看器型自述
   `ViewerHandler.viewModes` 那组**纯数据**（从前是一件会自己读 store 的 React 元素）。
3. **标签动作表六项**，两个开口（右键 / `Shift+F10`），详见 §7。「左移 / 右移」升格成全局命令。
4. **架子檐只剩「收起」、浮窗檐只剩 ✕**；被拿掉的四件进同一张叶菜单，由宿主自述一段
   `PaneHostChrome.menuRows` 递进来——**这只组件一个宿主名都不认识**，加第三种宿主时它一个字不改。
5. **换序不画浮影不画空位**：正本 §5 的两条贯穿规则补上「只对外来来源成立」。
6. **收账四条**：`focus-into` 的排队切到 `focus/after-commit.runAfterCommit`（那副队列的唯一产地）；
   `keymap/dispatch.ts` 那段「点瓦走 `clickDockIcon`」的过时注释改成事实；启动瓦 `dragRef()` 答
   `null` 时按**种类**再问一次驻留（`StageLauncher.residentKind` + `workbench/tree.firstRefOfKindIn`）；
   `FilesPanel` 补 `restingTarget`（选中行 → 第一行 → 根）且 `files-root` 自述 `focusInto: 'files'`。
7. **顺手修一处真 bug**：裁定 1 让顶栏标签条与**左架子那条条**横向重叠，而架子那条的 24px
   上下外扩正好够到顶栏里——`stripAt` 从前一遍扫描先到先得，于是指针明明在顶栏第 0 格上，
   判据交回「插进左架子第 1 位」。改成**两遍扫描**：先找真的含住这一点的，没有再找够得着的
   （`gate:drag` 场景 ① 当场抓到，`drop.test.ts` 三条守着）。

W6-a 与 W6-b 可以并行（前者动 store / kinds / items，后者动 ui/drag / tab-reorder / useTabDrag），合树接缝是 `LeafStrip`。W6-c 等两者合入。

### W7-d：W7 三批留下的三条真账（09-06，对着 §12.2 逐条结清）

不是新功能，是把 W7-c 交卷时挂在账上的三条办掉（详见 §12.2 那张表的一 / 二 / 五行）：

1. **浮窗最小尺寸由内容自述**。`StageItemSpec.floatMin?: {w,h}`——**内容自述、壳读表**：
   数与判词的产地是 `src/expose/float-min.ts`（会话总览自己说的 `{ w: 800 }`，与
   `--expose-rail-bp` 760 同源，`float-min.test.ts` 把四处写法对上账），名册那一行只是挂上去，
   `stage/items.floatMinOfItem` 是壳这一侧**唯一**的读表口，`transitions.freshFloatRect` /
   `defaultFloatRect` 只多收一对数——形态机那只纯函数里照旧一个瓦名都没有。身量取
   `max(默认, floatMin)`；装不下时缩到中央区那么大并贴中央区左上（`floatRectAt` 上的判词），
   锚与层叠一个字没动。开窗的**三条**路都读同一格自述：菜单 / 点瓦（`placement`）、
   撕成浮窗（`drop-commit`）、撕窗时那道轮廓预示（`useContentDrag`）——预示与落定不同源
   就不再是预示。**加第二块自述的面 = `STAGE_ITEMS` 那一行上多一格**，别处一个字不改。
2. **被预算收起的架子，空间回来自动展开**。`ShelfState.collapsedBy?: 'budget'` 记「谁收的」
   （不是「是不是自动的」——将来第三个来源加一格值即可）；`reclampShelves` 从一趟变两趟，
   次序相反：**归还按「先钉的先回」**（升序）、**让位按「后钉的先让」**（降序），合起来仍是
   同一条「先摆好的那份布局优先」。归还先跑，因为它只交地不占地。用户手动收 / 展一律清标记
   （`transitions.setShelfCollapsed` 与 `placement.setShelfCollapsed` 两口逐字同一条），
   所以**用户自己收起来的那条永远不会被替他展开**。
3. **`gate:perf` ⑤b 判据抗抖，且不抬预算**。时间那一半改判**这一趟的地板**（10 下里最快的
   那一下）；「最长主线程任务」那条降成参考打印。判词整段在 `perf-budget.tabPairSplitMs` 上。

门（本批加的三条都带反证）：`gate:a11y` 整门回绿（反证：拆掉名册那一格 `floatMin` →
`combobox` 当场回到 Tab 序，与 §12.2 记的两条红逐字相同）；`gate:layout` ③ 尾添
「700×500 → 1280×860 全部自己展开 + 厚度逐字不变」、⑤ 添「自述了 `floatMin` 的瓦开出来
≥ 那个尺寸」（反证：拆掉归还那一趟 → ③ 当场红）；`gate:squeeze` 步 `[6/11]` 里 W7-c 那段
**手动展开右架子删掉了**——步 `[7/11]` 还拖得到杆，就是裁定 2 的真机反证（反证：拆掉归还
那一趟 → 当场抛「厚度没拖到位：实际 -1.0px」，正是 W7-c 记的那句）。

---

## 11. 验收标准（代理可自证）

1. 三种来源（会话行、文件行、标签）拖到标签条上的每一个位置，屏幕上都有空位或描圈，浮影下都有一行字；这一条在 `gate:drag` 里对每种来源各断言一次。
2. 按下标签立刻切换内容，松手不动不再发生任何事；按下后横向移动 6px 才浮起；竖向 24px 才撕下。
3. 换序：拖到最左、最右、中间三处松手，顺序与活动位正确；宽标签能拖到窄邻居之后，且刚抬起那一帧邻居一个都不动；往右拖时右邻居在被拖标签右边缘越过它中心那一刻让位（不是中心对中心）；松手后有 150ms 的位移过渡（读到 `transition` 与非零起始位移）；中途 Esc 顺序不变、无残留 transform、无残留浮影；换序全程标签条与其它标签的底色逐字不变。
4. 往下压 6–24px 放到标签上 = 二合一：目标描圈、提示出现；回到条内回到换序；换序途中停住 1s 不发生任何事。
5. 文件单击三次 = 三个标签；第四次单击已开的 = 切过去。
6. 从架子里拖文件到聊天内容区中间 = 新标签，右带 = 与聊天并排；从浮窗里拖出去时浮窗不接住自己。
7. 目录面板：打开两个目录，架子标签条上两个目录名；同名目录带父目录后缀。
8. 一格都不重挂：换序、二合一、拆开、换比例四步内容根节点同一 DOM。

---

## 12. 不做与留账

- 三格及以上并排不做；两格的标签不能再并。
- 标签条溢出时的自动横滚（拖到条边缘让条滚动）留到有人真的开了二十个标签再做，今天先保证活动标签总在视野内（W3-b 已做）。
- 架子与浮窗里的树仍可通过右键菜单「分屏 ▸」得到多叶（W4 的能力），本方案不删这条菜单，但不在任何拖拽落点里提供分屏。
- 拖拽期间标签条不自动滚动、不跨窗口拖（多窗口本来就没有）。

### 12.1 留账（W6-c 交卷时的账，逐条待拍）

| # | 账 | 今天是什么 | 拍什么 |
| --- | --- | --- | --- |
| 一 | **「打开目录…」的原生目录对话框** | 一个路径输入框（结构化降级，判词整段在 `content/files/open-dir-hub.ts`，`gate:files` 有一档钉着它按得动） | 补它要动 `shell` 那格宿主口——而这台 React 壳今天注入的是 `shell: null`，`configureShellHost` 是一格**闩**：一旦声明，`hasShellHost()` 当场翻真，`GET /api/capabilities` 的 `shellTools` 与 oauth 域的 `openExternal` 两处判据跟着变。那是**用户可感知的行为改动**，按「行为裁定须先问」等拍板 |
| 二 | **`panel:files` 那块瓦拔不拔** | 留着。W6-a 之后「目录」瓦点开的是按目录多开的那一种（`files-root`），而 `panel:files` 那一档（面板内分栏）仍是「打开方式」七档之一、`gate:files` 第 5–6 步跑的就是它 | 拔掉 = 少一档打开方式、`gate:files` 两步要改写；留着 = 同一份文件树有两条进屏的路。它不影响任何 v3 的判据，所以 W6-c 一个字没动 |
| 三 | **边带分屏在架子 / 浮窗叶上的去留** | 拖拽落点里整支删了（§9 那行 W6-b），但**右键菜单「分屏 ▸」还在**（§12 第三条：那两处的分屏能力不删）。于是架子 / 浮窗上「分得出多叶，但拖不出来」 | 要么把菜单那四项也收掉（两处彻底跟中央区一致），要么给那两处补回一种拖拽分屏手势。今天这个中间态是**刻意**的：v3 只裁中央区 |
| 四 | **仓根 `bun run typecheck` 在 worktree 农场下有一条假红** | `packages/onething-runtime/src/toolkit/__tests__/support.ts(80,5) TS2322`：同一个 `Authorizer` 从两条路径各来一份 | 病根是 node_modules 农场：`node_modules` 整目录软链回主检出，而里面的 `@onething/core` 是**相对**软链 `../../packages/core`，解析出去落在主检出上；tsc 同时又编 worktree 自己的 `packages/core`。与本批无关（本批的 diff 一个字都不在 `packages/` 下），合树后在正常检出上复跑即可 |
| 五 | ~~**`ui/Tabs` 的 `onTabContextMenu` 今天只有拼贴台一个消费方**~~ | 仍是一个消费方，但那一口在 W7-c 收成了 `onTabMenu(id, {x,y})`（右键与 `Shift+F10` 两个来源，点由条自己量） | 形状复核了一次：交出去的是**结果**不是事件——键盘那条路没有光标，递事件就得伪造一发 MouseEvent |

### 12.2 留账（W7-c 交卷时的账；一 / 二 / 五由 **W7-d** 结清）

| # | 账 | 今天是什么 | 拍什么 |
| --- | --- | --- | --- |
| 一 | ~~**窄窗收起来的架子，窗子拉回去不会自己展开**~~ | **W7-d 裁定 2 结清**。让位时记一格 `ShelfState.collapsedBy: 'budget'`，`reclampShelves` 多跑一趟**归还**：预算又装得下就把记着这一格的展开并清标记，**用户自己收起来的一条都不碰**（手动收展一律清标记）。两趟次序相反且同出一条理由——归还按「先钉的先回」、让位按「后钉的先让」，合起来是「先摆好的那份布局优先」 | 已拍并落地。门：`gate:squeeze` 步 `[6/11]` 里那段**手动还原删掉了**（靠产品），步 `[7/11]` 拖得到杆就是它的真机反证；`gate:layout` ③ 尾添两条（700×500 → 1280×860 全部自己展开、厚度逐字不变） |
| 二 | ~~**会话总览摆成浮窗时落进窄档，侧栏没了、Tab 序多一格 combobox**~~ | **W7-d 裁定 1 结清**。修的既不是默认宽也不是断点：`StageItemSpec` 加一格 `floatMin?: {w,h}`，**内容自述、壳读表**——数与判词的产地是 `src/expose/float-min.ts`（`EXPOSE_FLOAT_MIN = { w: 800 }`，与 `--expose-rail-bp` 760 同源，`float-min.test.ts` 把四处写法对账），名册那一行只是挂上去，`stage/items.floatMinOfItem` 是壳这一侧唯一的读表口，`transitions.freshFloatRect` 只收一对数（形态机里一个瓦名都没有）。身量取 `max(默认, floatMin)`，装不下时缩到中央区那么大并贴中央区左上 | 已拍并落地。门：`gate:a11y` 那两条回绿（反证：拆掉名册上那一格 → `combobox` 当场回到 Tab 序）；`gate:layout` ⑤ 添一条「自述了 `floatMin` 的瓦开出来 ≥ 那个尺寸」（实测 800×480） |
| 三 | **`gate:perf` ⑤c 的强制排版从 5 涨到 6** | 已在 `perf-budget.tabReorderForcedLayouts` 抬到 6 并逐条署名。第 6 位是 **W7-t 加的 `ui/Tabs.clippedTabIds`**（那一批没回过 gate:perf） | 没有要拍的，是一笔**记账**：抬这一格的判法与 W6-p 当年 3→4 逐字同一条（有名有姓、确定性、便宜） |
| 四 | **`gate:perf` ④「core 进程 CPU 中位 ≥ 40%」** | 存量红，W6-p 交卷时已写明「反证回滚后照样 113%，与本批无关」。W7-c 三趟复量：103% / 64% / 100%；W7-d 复量 81% | 归 perf 线，不在 W7-c / W7-d |
| 五 | ~~**`gate:perf` ⑤b 贴着预算线抖**~~ | **W7-d 裁定 3 结清，而且没有抬预算**（351 一个字没动）：时间那一半改判**这一趟的地板**——10 下里最快的那一下。回归会把每一下都抬起来（地板同样抬），负载只抬尾巴（10 下里总有几下落在干净窗口里）。「最长主线程任务 ≤ 这一格」那条同理降成参考打印：它是整趟窗口里的单个极值，被污染得比 p95 更彻底。判词整段在 `src/perf-budget.ts` 的 `tabPairSplitMs` 上 | 已拍并落地。W7-d 实测正是这条裁定的现场：逐下 `381 266 244 243 246 235 256 245 244 252`——**p95 381 > 351 会红，地板 235 绿**，而强制排版 `2 2 2 …` 一次不抖 |
